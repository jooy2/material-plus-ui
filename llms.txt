# Material Plus

> A React component library implementing Material Design 3 — the components other Material libraries do not ship, and wider versions of the ones they do. Built on Base UI and Tailwind CSS v4, themed with CSS custom properties so it can coexist with an existing Material setup. ESM only, types included.

- Install with `npm install material-plus-ui`. ESM only, TypeScript declarations included.
- Peer dependencies: `@base-ui/react` 1, and `react` with `react-dom` 18 or 19. `lucide-react` ships inside the package.
- Setup is one stylesheet import — `material-plus-ui/styles.css`, or `material-plus-ui/tailwind.css` in a project already running Tailwind CSS v4 — and there is no provider to wrap the tree in.
- Every component is a named export from the package root and carries an `MP` prefix: `import { MPTextField } from 'material-plus-ui'`.
- Theming is plain CSS custom properties. `--mp-source-color` derives every colour role; `data-mp-scheme` selects light or dark and `data-mp-shape` moves the corner scale.
- Each component page below carries that component's full props table, so it is the page to read before writing a prop name.

## Guide

- [Getting started](https://material-plus.cdget.com/guide/getting-started): Material Plus is a React component library that implements Material Design 3. It follows the specification directly rather than wrapping somebody's…
- [Hooks](https://material-plus.cdget.com/guide/hooks): The machinery the library was already running, with a name you can import. Every one of these existed inside the components first; what is new is that your own…
- [App-wide defaults](https://material-plus.cdget.com/guide/config): A design that runs at size="sm" should be one decision, not one per call site. MPConfigProvider is where that decision goes.

## Design

- [Design language](https://material-plus.cdget.com/design/design-language): Material Plus implements the Material Design specification rather than wrapping somebody's implementation of it. That one decision is where almost everything…
- [Colour](https://material-plus.cdget.com/design/color): Material Plus generates its whole scheme from one source colour, the way Material Design does. Set that and every role follows; set a role when you disagree…
- [Prop conventions](https://material-plus.cdget.com/design/prop-conventions): Two rules govern everything below.
- [Localisation](https://material-plus.cdget.com/design/localization): Almost nothing in this library writes text a reader sees. What it does write — a calendar's month names, the name on a back arrow, the word on a button that…
- [Breakpoints](https://material-plus.cdget.com/design/breakpoints): Material Plus changes at Material's five window size classes , not at Tailwind's breakpoints. One ladder, read by the grid, the measures, the sidebar's…

## Components

- [All components](https://material-plus.cdget.com/components/): Everything released so far. The preview inside each card is not a picture — it is the component, running.

## Components: Examples

- [Overview](https://material-plus.cdget.com/examples/overview): Every part of the library on one screen. Not a grid of specimens — the components are laid out the way an application would lay them out, so you can see…
- [Landing page](https://material-plus.cdget.com/examples/concept-landing): A marketing page for Kestrel, a deploy tool that does not exist. It is the screen a component library is least obviously for — mostly type, space and one call…
- [Admin dashboard](https://material-plus.cdget.com/examples/concept-dashboard): The back office of Grange, a shop that does not exist. A rail, a filter row, four figures, a table with an action on every row and a resizable split under it —…
- [Sign-up page](https://material-plus.cdget.com/examples/concept-signup): Registration for Kestrel, in three steps. This is the library's fields with nothing else in the way — every kind of answer a form can ask for, and the states…

## Components: Display

- [MPAnchor](https://material-plus.cdget.com/components/display/anchor): The headings of the page being read, with the one the reader is in marked. A real &lt;nav&gt; of real fragment links, with the scroll tracking added on top…
- [MPAppLogo](https://material-plus.cdget.com/components/display/app-logo): A product's mark, at a known size, that is never an empty box. Artwork, an image, a pair of initials or the name itself — whichever of those exists, framed the…
- [MPAreaChart](https://material-plus.cdget.com/components/display/area-chart): A quantity over an ordered axis with the room under it filled in — a volume rather than a position. Stack them and the top edge becomes the total.
- [MPAvatar](https://material-plus.cdget.com/components/display/avatar): A picture of a person or a thing, at a known size, that is never an empty box. If the picture is missing or slow, the initials stand in; if there are no…
- [MPBadge](https://material-plus.cdget.com/components/display/badge): A small mark in the corner of something else: unread mail on an inbox icon, a status dot on an avatar, a count on a tab. With no children it lays out inline…
- [MPBarChart](https://material-plus.cdget.com/components/display/bar-chart): A measured length per category — how much, next to how much else. Grouped or stacked, upright or on its side, and always from zero.
- [MPBlockquote](https://material-plus.cdget.com/components/display/blockquote): Somebody else's words, set apart from your own. There is no state and no keyboard contract here — what there is, is markup that is easy to get wrong, and…
- [MPBreadcrumb](https://material-plus.cdget.com/components/display/breadcrumb): The trail of pages above the one being read. The last step is where the reader already is, so it is not a link at all — and a trail seven levels deep folds its…
- [MPChatBubble](https://material-plus.cdget.com/components/display/chat-bubble): One message in a conversation: the bubble, and everything a thread hangs around it — an avatar, a sender, a time, a delivery mark, a picture, an unfurled link.
- [MPChip](https://material-plus.cdget.com/components/display/chip): A compact token: a tag, a filter, a status, an entity plucked out of a list. It can be pressed, it can be selected, and it can carry its own delete affordance…
- [MPCodeBlock](https://material-plus.cdget.com/components/display/code-block): A viewer for one line of code or a thousand. Coloured, numbered, marked, copied — each off one prop, because the same component has to be a snippet inside a…
- [MPDataList](https://material-plus.cdget.com/components/display/data-list): A list of things and what they are called — a details panel, the summary of a record, the metadata under a heading. A real &lt;dl&gt; of real &lt;dt&gt; /…
- [MPDataTable](https://material-plus.cdget.com/components/display/data-table): A grid of data with the four things a reader does to one: put it in order, cut it down, step through it and take some of it away. Plus a file to keep.
- [MPDivider](https://material-plus.cdget.com/components/display/divider): A rule between two things. With no children it is a real role="separator" and nothing else; with children the line breaks around the label.
- [MPGaugeChart](https://material-plus.cdget.com/components/display/gauge-chart): One reading on a dial, with the trouble drawn on the face. The chart form worth arguing about — and the argument is on this page.
- [MPHeatmapChart](https://material-plus.cdget.com/components/display/heatmap-chart): A grid of cells coloured by how much — two categorical axes and a magnitude between them. For finding where the dense part is.
- [MPHighlight](https://material-plus.cdget.com/components/display/highlight): Marks the words a reader is looking for, inside text they were already reading. The component is the search, not just the styling: query is whatever a search…
- [MPIcon](https://material-plus.cdget.com/components/display/icon): A wrapper that gives an icon glyph a size and a colour. Material Plus draws no icons of its own, so the glyph comes from whichever set you chose — lucide-react…
- [MPImage](https://material-plus.cdget.com/components/display/image): A picture that says what it is doing — while it is on its way, and when it never arrives.
- [MPLineChart](https://material-plus.cdget.com/components/display/line-chart): A quantity over an ordered axis — how it has moved, and where it is going. Two axes, a grid, a legend and a hover layer, all of it drawn by hand and none of it…
- [MPList](https://material-plus.cdget.com/components/display/list): A stack of rows. The list is a sheet and the rows are what is on it — which is why size and color are set once, on the stack, and why a row that navigates is a…
- [MPPagination](https://material-plus.cdget.com/components/display/pagination): A row of pages, one of which is the one being read. Material has no pagination component, so what is Material here is everything around the decision: pills at…
- [MPPieChart](https://material-plus.cdget.com/components/display/pie-chart): Parts of a whole, at a glance. The narrowest chart here and the easiest one to misuse — it answers one question, and a bar chart answers the rest.
- [MPPill](https://material-plus.cdget.com/components/display/pill): A floating lozenge holding a small amount of live information — a call in progress, an upload that is still going, a recording, a train two minutes away.
- [MPScatterChart](https://material-plus.cdget.com/components/display/scatter-chart): Two measures against each other, one mark per observation. The only chart here with a value axis in both directions.
- [MPShortcut](https://material-plus.cdget.com/components/display/shortcut): A keyboard key, or a combination of them. Two things make it more than a styled &lt;kbd&gt; , and both are about the label rather than the box around it.
- [MPSparkline](https://material-plus.cdget.com/components/display/sparkline): A series as a mark, in the space of a word. No axes, no legend, and nothing to read a value against — it answers which way this has been going, and nothing…
- [MPSpoiler](https://material-plus.cdget.com/components/display/spoiler): Content that is covered until somebody asks for it. A plot twist, a salary range, a photograph with a warning on it — visible as a shape, unreadable by…
- [MPStatistic](https://material-plus.cdget.com/components/display/statistic): One figure, said once and read from across a desk — with what it is, and whether it is going the right way.
- [MPStepper](https://material-plus.cdget.com/components/display/stepper): A sequence being worked through, with one panel at a time.
- [MPTable](https://material-plus.cdget.com/components/display/table): A grid of data, described by its columns rather than written out row by row. The header and the cells cannot drift apart, because there is only one place that…
- [MPTextLink](https://material-plus.cdget.com/components/display/text-link): A link, in a sentence or on its own. Everything about it is deliberately smaller than a button — no surface, no height of its own, no colour unless asked. What…
- [MPTimeline](https://material-plus.cdget.com/components/display/timeline): A sequence of steps, in the order they happen in. One prop says how far reality has got, and every bullet, line and heading follows from it.
- [MPTimelineChart](https://material-plus.cdget.com/components/display/timeline-chart): What ran when, one row per thing. A Gantt: rows down the side, a calendar along the bottom, and a bar for every stretch.
- [MPTreeView](https://material-plus.cdget.com/components/display/tree-view): A tree of rows that open and shut. One widget with one tab stop, walked with the arrow keys — a file explorer, a category picker, a documentation sidebar.
- [MPTypography](https://material-plus.cdget.com/components/display/typography): Text at one of Material's type roles. The type scale is the one thing in a design system everything else is measured against — this is that ladder on its own,…
- [MPVisuallyHidden](https://material-plus.cdget.com/components/display/visually-hidden): Text for a screen reader and nobody else. The sentence behind a bare number, a lone glyph, or a sort arrow.

## Components: Feedback

- [MPAlert](https://material-plus.cdget.com/components/feedback/alert): A message about something that happened, set into the page it is about. One component with different slots filled — a bare line, a line with a glyph, or a…
- [MPDialog](https://material-plus.cdget.com/components/feedback/dialog): A sheet that takes the page away until it is answered. The sections are props rather than sub-components to compose: the arrangement of a dialog is fixed —…
- [MPEmpty](https://material-plus.cdget.com/components/feedback/empty): What stands where content would have been: a glyph, a headline, a sentence and a way out. It is the other half of MPSkeleton — that is the shape of something…
- [MPHoverCard](https://material-plus.cdget.com/components/feedback/hover-card): A card that opens when the pointer rests on something, holding a preview of what is on the other side of it. Uninvited like a tooltip, reachable like a popover.
- [MPMeter](https://material-plus.cdget.com/components/feedback/meter): How much of something there is, on a scale known in advance — disk used, seats taken, quota spent, a password's strength. It looks like a progress bar and…
- [MPOverlay](https://material-plus.cdget.com/components/feedback/overlay): A sheet over the whole page that stops it being used. What separates it from a dialog is what is not here: no surface, no outline, no title, no actions — the…
- [MPPopconfirm](https://material-plus.cdget.com/components/feedback/popconfirm): A confirmation that stays where the control is.
- [MPPopover](https://material-plus.cdget.com/components/feedback/popover): A sheet that opens beside the thing that opened it — and, unlike a tooltip, one you can reach: it stays up until it is dismissed, and what is inside it can be…
- [MPProgressBox](https://material-plus.cdget.com/components/feedback/progress-box): A row of segments that light up. A bar and a ring both say “this much is done” — a quantity a reader measures; four segments say “this is step three”, which is…
- [MPProgressCircular](https://material-plus.cdget.com/components/feedback/progress-circular): A ring that fills, and the one to reach for where there is no room for a bar — inside a button, at the end of a table row, next to a field.
- [MPProgressLinear](https://material-plus.cdget.com/components/feedback/progress-linear): A bar that fills, and the workhorse of the three progress indicators — the only one that shows how much is left at a glance, because length is the one quantity…
- [MPSkeleton](https://material-plus.cdget.com/components/feedback/skeleton): The shape of something that has not loaded yet. It reserves the space the real thing will take, which is the whole job — a card that grows by 200px when its…
- [MPSnackbar](https://material-plus.cdget.com/components/feedback/snackbar): A short message about something that has already happened, raised from anywhere in the application. Wrap the tree once in MPSnackbarProvider ; every call site…
- [MPTooltip](https://material-plus.cdget.com/components/feedback/tooltip): A short label that appears when the pointer rests on something. The whole component is a wrapper — it adds no element to the layout, and the child stays…
- [MPTour](https://material-plus.cdget.com/components/feedback/tour): A guided walk over a page that already exists: a few steps, each anchored to something on screen, with the dimming cut away around it.
- [UseMPConfirm](https://material-plus.cdget.com/components/feedback/confirm): “Are you sure?”, as a promise. A click handler asks, and gets a boolean back.

## Components: Inputs

- [MPButton](https://material-plus.cdget.com/components/inputs/button): Material Design's five buttons — filled, tonal, elevated, outlined and text — across four accent families and the library's size ladder. The state layer, the…
- [MPButtonGroup](https://material-plus.cdget.com/components/inputs/button-group): A run of buttons that belong together. The corners that face a neighbour are cut back, and the variant, size, colour and disabled state are set once for the…
- [MPCalendar](https://material-plus.cdget.com/components/inputs/calendar): A month on the page, rather than in a popup. The same grid MPDatePicker opens, with nothing in front of it.
- [MPCheckbox](https://material-plus.cdget.com/components/inputs/checkbox): A single yes or no. The box is on-surface-variant at rest and fills with the accent family when it is ticked, with Material's 40dp state layer around an 18dp…
- [MPColorPicker](https://material-plus.cdget.com/components/inputs/color-picker): A colour, chosen by eye. A saturation square with a hue rail beside it — the arrangement every design tool has settled on, because it is the one that puts…
- [MPCombobox](https://material-plus.cdget.com/components/inputs/combobox): A field you can type into and also choose from. The shell is MPTextField ’s wearing a chevron, exactly as MPSelect ’s trigger is — what is different is what…
- [MPCommandPalette](https://material-plus.cdget.com/components/inputs/command-palette): Everything an application can do, behind one field.
- [MPDatePicker](https://material-plus.cdget.com/components/inputs/date-picker): One day, chosen from a calendar. The trigger is a text field's shell wearing a calendar glyph, so a date field in a form is the same object as the fields…
- [MPDateRangePicker](https://material-plus.cdget.com/components/inputs/date-range-picker): A span between two days. Two months side by side, because a range that crosses a month boundary is the ordinary case and a one-month picker makes it a…
- [MPDateTimePicker](https://material-plus.cdget.com/components/inputs/date-time-picker): A day and a time, in one popup. Not a date picker that grew a clock — the two panels sit side by side at exactly the same height, so the popup is one rectangle…
- [MPFieldset](https://material-plus.cdget.com/components/inputs/fieldset): A group of controls that answer one question together, with a name on it.
- [MPFilePicker](https://material-plus.cdget.com/components/inputs/file-picker): A box you drop files on, or click to open the file dialog. Material has no component for this, so what it borrows is the part that is a surface — the corner…
- [MPFloatingActionButton](https://material-plus.cdget.com/components/inputs/floating-action-button): The one action a screen is about, floating over it. Material's three containers, its three sizes with the corner each of them takes, level-3 elevation and the…
- [MPForm](https://material-plus.cdget.com/components/inputs/form): A &lt;form&gt; that knows which of its fields is wrong.
- [MPIconButton](https://material-plus.cdget.com/components/inputs/icon-button): A round button with a glyph in it and nothing else. Almost all of it is MPButton ; what it adds is the one thing that cannot be defaulted and is always missing…
- [MPMenu](https://material-plus.cdget.com/components/inputs/menu): A list of actions that appears when something is pressed. The rows are composed rather than passed as data — the opposite of MPSelect , and deliberately.
- [MPMenubar](https://material-plus.cdget.com/components/inputs/menubar): The strip of words at the top of an application — File, Edit, View — each of which opens a menu.
- [MPNumberField](https://material-plus.cdget.com/components/inputs/number-field): A field that only holds a number, wearing MPTextField's shell to the pixel. Arrow keys and steppers move by step, the value clamps to min and max, and format…
- [MPOtpField](https://material-plus.cdget.com/components/inputs/otp-field): A row of one-character slots: a PIN, a texted verification code, an invite key. One hidden value behind however many inputs.
- [MPRadioGroup](https://material-plus.cdget.com/components/inputs/radio-group): A set of options where exactly one is chosen. Material's 20dp ring with a 10dp fill inside it, and — the part that matters — one tab stop for the whole set,…
- [MPRating](https://material-plus.cdget.com/components/inputs/rating): A score out of five, as a row of stars. Underneath is a radio group of real inputs, one per choosable score — which is what makes the row one tab stop, the…
- [MPSegmentedButton](https://material-plus.cdget.com/components/inputs/segmented-button): Two to five choices in one pill. A hairline container with the segments divided by more of the same, and the chosen one filled with secondary-container —…
- [MPSelect](https://material-plus.cdget.com/components/inputs/select): One value chosen from a list. The trigger is MPTextField's shell wearing a chevron — the same notched outline, the same label in the notch, the same supporting…
- [MPSlider](https://material-plus.cdget.com/components/inputs/slider): A value chosen along a range. The active part of the track is the accent family and the rest is surface-container-highest, which is Material's own pairing and…
- [MPSwitch](https://material-plus.cdget.com/components/inputs/switch): An on and off that takes effect the moment it moves. Material's 52×32 track holding a 16dp thumb that grows to 24dp once it is on — the growth is what makes…
- [MPTextField](https://material-plus.cdget.com/components/inputs/text-field): A Material Design outlined text field that survives an IME, with the label, supporting text, adornments and password toggle already assembled. Every colour,…
- [MPTimePicker](https://material-plus.cdget.com/components/inputs/time-picker): A time of day, chosen from columns. The bounds are checked at the granularity of the column being drawn, which is the detail that separates a working time…
- [MPToggle](https://material-plus.cdget.com/components/inputs/toggle): A button that stays down. Off is neutral, on is the accent — and what it changes is the state of the thing beside it.
- [MPTransfer](https://material-plus.cdget.com/components/inputs/transfer): Two lists and the arrows between them: everything that could be chosen on one side, everything that has been on the other.
- [MPTreeSelect](https://material-plus.cdget.com/components/inputs/tree-select): A value chosen from a tree rather than from a list. The gap between a select and a tree view: a hierarchy that folds away behind a field.

## Components: Layout

- [MPAccordion](https://material-plus.cdget.com/components/layout/accordion): A stack of sections, one of which is open. Opening the next closes the last, which is the whole reason it is not simply a stack of collapsibles.
- [MPAspectRatio](https://material-plus.cdget.com/components/layout/aspect-ratio): A box that keeps a proportion whatever width it is given. It draws nothing — what it does is reserve the space, so a card whose image arrives late does not…
- [MPBottomNavigation](https://material-plus.cdget.com/components/layout/bottom-navigation): A row of destinations held against the bottom edge of the window. Material calls this the navigation bar , and what is drawn is the specification's: an 80dp…
- [MPBox](https://material-plus.cdget.com/components/layout/box): A sheet with content on it. The plainest surface in the library: it groups things, and that is all it does.
- [MPCard](https://material-plus.cdget.com/components/layout/card): An MPBox with the parts a card is made of laid out on it: a picture, a heading, a subheading, a body and a footer. MD3's own card anatomy, in slots.
- [MPCarousel](https://material-plus.cdget.com/components/layout/carousel): A strip of slides, one of which is in view. Built on the browser's own scrolling with CSS snap points, so swiping, trackpad dragging and the arrow keys all…
- [MPCollapsible](https://material-plus.cdget.com/components/layout/collapsible): One section that folds, standing on its own. A "Show more" on a form, an optional block of settings, the detail under a row — with the panel's height animated…
- [MPContainer](https://material-plus.cdget.com/components/layout/container): The page margin, and optionally a measure. Material holds content off the edge of the window by 16dp in a compact window and 24dp from medium up — this is that…
- [MPDrawer](https://material-plus.cdget.com/components/layout/drawer): A panel attached to one edge of the window. Two things in one component, because they are the same panel: the drawer you open, and the drawer that is simply…
- [MPFlex](https://material-plus.cdget.com/components/layout/flex): A row, or a column, and the width at which it changes from one to the other. It draws nothing — no surface, no padding, no corner — only the five properties a…
- [MPFloatingBottomNavigation](https://material-plus.cdget.com/components/layout/floating-bottom-navigation): A row of destinations floating clear of the bottom edge. The same bar as MPBottomNavigation , lifted off the page so the content keeps going underneath it.
- [MPFooter](https://material-plus.cdget.com/components/layout/footer): The sheet at the end of a page. A real &lt;footer&gt; , which at the top level of a document is the contentinfo landmark.
- [MPGrid](https://material-plus.cdget.com/components/layout/grid): Material's layout grid: a row divided into columns, and items that take some of them. The column count changes with the window size class the specification…
- [MPHeader](https://material-plus.cdget.com/components/layout/header): The bar across the top of a page. MD3's top app bar, with a site's own three regions on it — the mark, the middle, the actions.
- [MPMockup](https://material-plus.cdget.com/components/layout/mockup): A picture of a device with a real page inside it. The content is laid out against the machine's own resolution and the whole device is then scaled once to fit…
- [MPNavigationMenu](https://material-plus.cdget.com/components/layout/navigation-menu): A site's navigation: a row of destinations, some of which open a panel of more of them.
- [MPPageLayout](https://material-plus.cdget.com/components/layout/page-layout): The skeleton a page is hung on: a header, a footer, one sidebar or two, and the content between them. What it is really for is the landmarks.
- [MPPanes](https://material-plus.cdget.com/components/layout/panes): A set of panes with draggable handles between them. The split is described in fractions, so it survives the window being resized without a single line of…
- [MPPortal](https://material-plus.cdget.com/components/layout/portal): Children rendered somewhere else in the DOM, usually the end of &lt;body&gt; . For a subtree that has to escape a clipping or stacking context its own position…
- [MPScrollArea](https://material-plus.cdget.com/components/layout/scroll-area): A box with a scrollbar of its own. The browser's is drawn by the operating system — a different width on every machine and a different colour from the sheet it…
- [MPScrollZone](https://material-plus.cdget.com/components/layout/scroll-zone): A strip of anything, laid out in one direction and scrolled in it. Cards, chips, avatars or thumbnails run across the box or down it, in as many lines as you…
- [MPShow](https://material-plus.cdget.com/components/layout/show): Content at some window sizes and not others. A rail on a laptop and a bottom bar on a phone, written as two components with a boundary between them rather than…
- [MPSidebar](https://material-plus.cdget.com/components/layout/sidebar): A column beside the page's content, and a drawer once the window is too narrow to hold one. MD3's standard and modal navigation drawers, as one component.
- [MPStack](https://material-plus.cdget.com/components/layout/stack): Things laid over each other in a pile. A stack of avatars, a deck of cards, a heap of documents, a run of overlapped thumbnails — one component, because…
- [MPTabs](https://material-plus.cdget.com/components/layout/tabs): One set of panels, one of which is shown. Material's two kinds of tab are here as variant — primary for the top level of a screen and secondary for a division…
- [MPToolbar](https://material-plus.cdget.com/components/layout/toolbar): A bar of controls: an application header, a page's action row, the strip along the bottom of an editor. Three slots and a row.

## Components: Motion

- [MPAnimateAppear](https://material-plus.cdget.com/components/motion/animate-appear): A list of things settling into place one after another. The effect belongs to the set rather than to any one item, which is what walks a reader's eye down it…
- [MPAnimateBlink](https://material-plus.cdget.com/components/motion/animate-blink): Content pulsing between full opacity and a floor. A recording light, a live badge, a count that has just changed — and the one effect here that should be…
- [MPAnimateCounter](https://material-plus.cdget.com/components/motion/animate-counter): A number counting up to its value. The one effect here that cannot be a keyframe on its own — and it is still a CSS animation.
- [MPAnimateFade](https://material-plus.cdget.com/components/motion/animate-fade): Content arriving or leaving on opacity alone. Nothing moves, so nothing reflows and no text is resampled — the one entrance that is safe on a block of prose at…
- [MPAnimateFloat](https://material-plus.cdget.com/components/motion/animate-float): An endless, slow drift. The only thing in the set that says not fixed to the page — a shadow is a claim about depth, and this is depth behaving.
- [MPAnimateGrow](https://material-plus.cdget.com/components/motion/animate-grow): Content unfolding from a point. A panel out of a toolbar, a card out of the row it sits in, a sheet out of the button that summoned it.
- [MPAnimateHeadline](https://material-plus.cdget.com/components/motion/animate-headline): One line replacing the one above it, on a timer. A set of phrases where any one of them would have done — three ways of saying what a product is, a rotating…
- [MPAnimateLighting](https://material-plus.cdget.com/components/motion/animate-lighting): A light travelling around the outside of something. The way to say "this one, now" without moving anything — the row that is processing, the field being…
- [MPAnimateMarquee](https://material-plus.cdget.com/components/motion/animate-marquee): Content scrolling steadily past, forever. Two copies laid end to end, moving at the pace of a reader rather than at whatever pace fits the box.
- [MPAnimateReveal](https://material-plus.cdget.com/components/motion/animate-reveal): Content uncovered where it already is. The only entrance in the set where nothing moves and no colour changes — the ink is final from the first frame, and what…
- [MPAnimateRotate](https://material-plus.cdget.com/components/motion/animate-rotate): Content turning about a point. Two angles rather than one, which is what lets one component be both a swing into place and a spin that never lands.
- [MPAnimateScramble](https://material-plus.cdget.com/components/motion/animate-scramble): Text settling out of noise. MPAnimateTyping 's sibling, and the difference is what the box does: this one is its finished length from the first frame.
- [MPAnimateShake](https://material-plus.cdget.com/components/motion/animate-shake): The answer to something that did not work. Four hundred milliseconds, said once, and it says that was refused more plainly than any colour does.
- [MPAnimateSlide](https://material-plus.cdget.com/components/motion/animate-slide): Content travelling in from one edge. The element moves, the layout does not — nothing on the page reflows while it runs.
- [MPAnimateSplit](https://material-plus.cdget.com/components/motion/animate-split): A line arriving a word or a character at a time. MPAnimateAppear for a string — the same settling, over the pieces of a sentence rather than over children a…
- [MPAnimateTyping](https://material-plus.cdget.com/components/motion/animate-typing): Text appearing one character at a time. The whole string is in the document from the first frame, so nothing reflows and nobody is kept waiting on the…
- [MPAnimateZoom](https://material-plus.cdget.com/components/motion/animate-zoom): Content arriving from the middle of where it will end up. The effect for the one thing on a screen that is meant to interrupt — a confirmation, a result, a…

## Optional

- [Changelog](https://material-plus.cdget.com/changelog): Every release of Material Plus, newest first — what was added, and what changed.
- [한국어](https://material-plus.cdget.com/ko/llms.txt): The same documentation in another language.
- [GitHub](https://github.com/jooy2/material-plus): The source of every component, its tests, and the issue tracker.
- [npm](https://www.npmjs.com/package/material-plus-ui): The published package.
