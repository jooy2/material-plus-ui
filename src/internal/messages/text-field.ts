import type { MPNamespace } from '../i18n';

/** `MPTextField`'s reveal toggle — see `MPMessages['textField']`. */
export const TEXT_FIELD: MPNamespace<'textField'> = {
  name: 'textField',
  en: {
    showPassword: 'Show the password',
    hidePassword: 'Hide the password'
  }
};
