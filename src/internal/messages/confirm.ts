import type { MPNamespace } from '../i18n';

/** The buttons a confirmation draws when nobody names them — see `MPMessages['confirm']`. */
export const CONFIRM: MPNamespace<'confirm'> = {
  name: 'confirm',
  en: {
    confirm: 'Confirm',
    cancel: 'Cancel',
    ok: 'OK'
  }
};
