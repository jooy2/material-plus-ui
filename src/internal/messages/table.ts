import type { MPNamespace } from '../i18n';

/** `MPTable`'s empty row — see `MPMessages['table']`. */
export const TABLE: MPNamespace<'table'> = {
  name: 'table',
  en: {
    empty: 'No data'
  }
};
