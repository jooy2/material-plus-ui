import type { MPNamespace } from '../i18n';

/** `MPSparkline`'s one sentence — see `MPMessages['sparkline']`. */
export const SPARKLINE: MPNamespace<'sparkline'> = {
  name: 'sparkline',
  en: {
    summary: '{count} points, from {first} to {last}'
  }
};
