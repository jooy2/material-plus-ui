import type { MPNamespace } from '../i18n';

/** `MPCombobox`'s two drawn lines — see `MPMessages['combobox']`. */
export const COMBOBOX: MPNamespace<'combobox'> = {
  name: 'combobox',
  en: {
    empty: 'No matches',
    add: 'Add \u201C{label}\u201D'
  }
};
