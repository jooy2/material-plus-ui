import type { MPNamespace } from '../i18n';

/** `MPBreadcrumb`'s two names — see `MPMessages['breadcrumb']`. */
export const BREADCRUMB: MPNamespace<'breadcrumb'> = {
  name: 'breadcrumb',
  en: {
    label: 'Breadcrumb',
    expand: 'Show hidden steps'
  }
};
