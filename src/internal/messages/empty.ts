import type { MPNamespace } from '../i18n';

/** `MPEmpty`'s heading — see `MPMessages['empty']`. */
export const EMPTY: MPNamespace<'empty'> = {
  name: 'empty',
  en: {
    title: 'Nothing here'
  }
};
