import type { MPNamespace } from '../i18n';

/** `MPTextLink`'s spoken warning — see `MPMessages['textLink']`. */
export const TEXT_LINK: MPNamespace<'textLink'> = {
  name: 'textLink',
  en: {
    newTab: 'Opens in a new tab'
  }
};
