import type { MPNamespace } from '../i18n';

/** `MPAlert`'s dismiss button — see `MPMessages['alert']`. */
export const ALERT: MPNamespace<'alert'> = {
  name: 'alert',
  en: {
    dismiss: 'Dismiss'
  }
};
