import type { MPNamespace } from '../i18n';

/** `MPAnchor`'s one name — see `MPMessages['anchor']`. */
export const ANCHOR: MPNamespace<'anchor'> = {
  name: 'anchor',
  en: { label: 'On this page' }
};
