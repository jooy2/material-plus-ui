import type { MPNamespace } from '../i18n';

/** `MPOverlay`'s own name — see `MPMessages['overlay']`. */
export const OVERLAY: MPNamespace<'overlay'> = {
  name: 'overlay',
  en: {
    label: 'Overlay'
  }
};
