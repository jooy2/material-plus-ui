import type { MPNamespace } from '../i18n';

/** `MPFilePicker`'s prompt — see `MPMessages['filePicker']`. */
export const FILE_PICKER: MPNamespace<'filePicker'> = {
  name: 'filePicker',
  en: {
    prompt: 'Drop files here, or click to browse'
  }
};
