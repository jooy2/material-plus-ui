import{_ as r,C as t,o as h,c as d,j as a,a as l,E as s,a4 as i}from"./chunks/framework.BoUsC9QI.js";const y=JSON.parse('{"title":"MPLineChart","description":"A quantity over an ordered axis — how it has moved, and where it is going. Two axes, a grid, a legend and a hover layer, all of it drawn by hand and none of it…","frontmatter":{"title":"MPLineChart","order":32},"headers":[],"relativePath":"components/charts/line-chart.md","filePath":"en/components/charts/line-chart.md","lastUpdated":1789967010000}'),c={name:"components/charts/line-chart.md"};function p(g,e,u,w,k,b){const o=t("Demo"),n=t("PropsTable");return h(),d("div",null,[e[0]||(e[0]=a("h1",{id:"mplinechart",tabindex:"-1"},[l("MPLineChart "),a("a",{class:"header-anchor",href:"#mplinechart","aria-label":'Permalink to "MPLineChart"'},"​")],-1)),e[1]||(e[1]=a("p",{class:"mp-lede"},"A quantity over an ordered axis — how it has moved, and where it is going. Two axes, a grid, a legend and a hover layer, all of it drawn by hand and none of it a dependency.",-1)),s(o,{src:"line-chart/hero",minHeight:1420}),e[2]||(e[2]=i(`<div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MPLineChart } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPLineChart</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  categories</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Mon&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Tue&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Wed&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Thu&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Fri&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  series</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[{ name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Signups&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, data: [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">120</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">138</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">131</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">164</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">190</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] }]}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;;</span></span></code></pre></div><h2 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-label="Permalink to &quot;Props&quot;">​</a></h2>`,2)),s(n,{name:"MPLineChart"}),e[3]||(e[3]=i('<h2 id="the-axis-does-not-start-at-zero" tabindex="-1">The axis does not start at zero <a class="header-anchor" href="#the-axis-does-not-start-at-zero" aria-label="Permalink to &quot;The axis does not start at zero&quot;">​</a></h2><p>A bar&#39;s <strong>length</strong> means its value, so a bar chart cut off above zero is a lie about proportion. A line&#39;s <strong>position</strong> means its value, and nothing about the mark claims proportionality — cropping the scale moves every point by the same amount and the picture survives.</p><p>Forcing zero onto a series that runs between 3,200 and 3,400 draws a flat line, and a flat line reports a real change as nothing happening. So this chart leaves zero out and <code>MPBarChart</code> will not.</p><p>Pass a <code>yAxis</code> with <code>min: 0</code> where zero genuinely is the baseline — and reach for <a href="./area-chart">MPAreaChart</a> where the quantity under the line is the point, since a fill has to measure from zero.</p><h2 id="a-gap-is-a-gap" tabindex="-1">A gap is a gap <a class="header-anchor" href="#a-gap-is-a-gap" aria-label="Permalink to &quot;A gap is a gap&quot;">​</a></h2><p>A <code>null</code> is a point nothing was measured at, and the line breaks there. It is not joined across, because a straight run through values nobody has is the one kind of invented data a reader never questions: it looks exactly like the rest of the line.</p><p><code>smooth</code> is a monotone fit, so a rising run never turns back on its way up and the lowest number in the data is the lowest point on screen. An ordinary spline through the same points overshoots both, and a chart that draws a dip has reported one.</p><p><code>gaps</code> is there for the two cases where breaking is the wrong reading. <code>gaps=&quot;connect&quot;</code> joins the two sides and draws the joining run <strong>dashed</strong> — the line carries on, and the picture still says which part of it was measured. <code>gaps=&quot;zero&quot;</code> draws the missing reading at zero and widens the value axis to hold it, which is right where a gap means &quot;none of it happened&quot; and wrong where it means &quot;nobody was counting&quot;.</p><p>Only the drawing changes. The hover panel and the table behind the chart report a gap as a gap in all three, because they are the copy of the data a reader gets when they need the number itself.</p><h2 id="a-target-is-a-reference-not-a-series" tabindex="-1">A target is a reference, not a series <a class="header-anchor" href="#a-target-is-a-reference-not-a-series" aria-label="Permalink to &quot;A target is a reference, not a series&quot;">​</a></h2><p>A reference on <code>yAxis</code> — <code>{ value: 300, label: &#39;SLO&#39;, color: &#39;error&#39; }</code> — draws a dashed line across the plot at 300 and writes its name at the end of it. Give a reference a <code>to</code> as well and it is a <strong>band</strong> instead — the range a reading should stay inside, the window something was down for — with both edges drawn and the room between them filled.</p><p>It is deliberately not data. A reference is a fact the reader brought with them, so it wears the chrome&#39;s ink rather than a palette slot, it is dashed so it cannot be mistaken for a gridline, and the chart never invents one. <code>color</code> takes a role for the case worth having: a threshold somebody is watching is exactly what <code>error</code> is for.</p><p><strong>The scale opens to hold it.</strong> A target above everything measured would otherwise be cropped out of the picture, which is the one thing a target must never be. Pin <code>min</code> or <code>max</code> to stop that.</p><p>A band is drawn <strong>under</strong> the marks and a line <strong>over</strong> them. A fill over a bar is a bar the reader has to look through; a target hidden behind the series that crossed it is a target nobody can read.</p><p>References belong to the axis whose scale their <code>value</code> is on, and they are drawn perpendicular to it — so the same reference is a horizontal line upright and a vertical one on a chart turned on its side. An axis of names has no place between two categories for a line to be at, so only an axis with a scale reads them: the value axis always, and the category axis where it runs on numbers, as <a href="./scatter-chart">MPScatterChart</a>&#39;s does. A <code>value</code> may be a <code>Date</code> on an axis of dates.</p><p>A named reference is read out with the chart, beside the table, because the table has no column for it.</p><h2 id="the-hover-layer-is-on-by-default" tabindex="-1">The hover layer is on by default <a class="header-anchor" href="#the-hover-layer-is-on-by-default" aria-label="Permalink to &quot;The hover layer is on by default&quot;">​</a></h2><p>A chart drawn in a browser is a thing a reader interrogates. The question a line chart gets asked is &quot;what happened in March&quot;, and answering it by making somebody measure against a gridline is answering it badly — so the crosshair and the panel are there without being asked for.</p><p>The <strong>whole column</strong> is the hit target, not the line. A two-pixel stroke is not something a pointer can be asked to land on, and the nearest category is what a reader hovering the empty space above a point means.</p><p>Set <code>tooltip={false}</code> only if the numbers are readable another way. The table below is always one of those ways.</p><p><code>sort: &#39;value&#39;</code> on <code>tooltip</code> puts the largest row first, which is what a chart of eight series wants: the row a reader is looking for is usually the big one, and hunting for it down a list in a fixed order is the work the panel exists to save. The default is the order the series were passed, so a reader who has learned the legend can find a row without reading it. Ties keep their series order either way.</p><p>A <strong>stack</strong> also gets a row for what the column adds up to, ruled off from the parts and carrying no swatch, because the height of that column is the whole point of the shape and a panel that listed the parts without it would leave the reader adding them up. An unstacked chart does not: its series are not parts of anything, and adding them would be the panel inventing a quantity. Neither does a percent stack, whose answer is 100% at every category. A <code>total</code> of <code>false</code> or <code>true</code> on <code>tooltip</code> overrides both.</p><h2 id="dragging-a-range-out-of-a-long-axis" tabindex="-1">Dragging a range out of a long axis <a class="header-anchor" href="#dragging-a-range-out-of-a-long-axis" aria-label="Permalink to &quot;Dragging a range out of a long axis&quot;">​</a></h2><p><code>zoom</code> is for the chart with ninety points on it, where the shape of one week is inside a picture of a quarter. A reader drags across the plot, the chart redraws to what they picked, and the scale re-fits — so the window is a <strong>chart of that range</strong> rather than a magnified picture of the whole one.</p><p>Everything follows the window: the value scale, the hover panel, the spoken readout and the table behind the picture. The table matters most. It is what a reader who cannot see the plot is given <strong>instead</strong> of it, so one still listing ninety points would be describing a chart that is not on the page.</p><p>A drag narrower than two categories is ignored, because a window of one category has no distance left in it for a line to have travelled — a click on the plot is exactly that, and is left alone. <code>Escape</code> clears the reading first and the window second, which is the order the reader made them in.</p><p>The way back is a <strong>Show all</strong> control over the corner of the plot, and it is a sibling of the picture rather than a child: <code>role=&quot;img&quot;</code> is a leaf role, so a button inside it is one no keyboard can reach and no screen reader can find. It is named in every language the library ships.</p><p>Pass an object to control the window yourself — <code>range</code> and <code>onRangeChange</code> for a controlled one, <code>defaultRange</code> to open on a window, <code>min</code> to require more than two categories. A range is clamped to the data rather than trusted: one that outlives the series it was picked from would otherwise be an empty chart with no way back to the full one.</p><p>Only a chart whose categories are a row of slots takes it. <a href="./scatter-chart">MPScatterChart</a> has two value axes and no row to cut, so it crops with <code>xAxis</code> and <code>yAxis</code> instead, and a pie has no axis at all.</p><p>The drag is a pointer gesture and there is no keyboard equivalent. That is a gap this component does not pretend to close: what a keyboard reader has instead is the arrow keys walking every column and the table behind the picture, neither of which a window would improve. A caller who needs the window under a keyboard&#39;s control owns <code>range</code> and can put it behind a control of their own.</p><h2 id="everything-a-pointer-does-a-keyboard-does" tabindex="-1">Everything a pointer does, a keyboard does <a class="header-anchor" href="#everything-a-pointer-does-a-keyboard-does" aria-label="Permalink to &quot;Everything a pointer does, a keyboard does&quot;">​</a></h2><p>The plot is one tab stop. Left and right walk the columns, <code>Home</code> and <code>End</code> jump to the ends, <code>Escape</code> clears the reading. Each column is announced by a clipped live region that is a <strong>sibling</strong> of the picture rather than a child — <code>role=&quot;img&quot;</code> is a leaf role, so everything inside it is cut out of the accessibility tree and a live region in there would announce to nobody.</p><h2 id="the-numbers-are-always-behind-the-picture" tabindex="-1">The numbers are always behind the picture <a class="header-anchor" href="#the-numbers-are-always-behind-the-picture" aria-label="Permalink to &quot;The numbers are always behind the picture&quot;">​</a></h2><p>Every chart renders the data as a table, clipped rather than drawn, and points <code>aria-describedby</code> at it. A reader who cannot see the plot gets the data itself rather than a summary of it, and a reader who can is not made to scroll past a table they did not ask for.</p><p><code>label</code> is what the chart is a chart <strong>of</strong>. It is the accessible name and the table&#39;s caption, so it is worth writing even though nothing breaks without it.</p><h2 id="colour-follows-the-entity" tabindex="-1">Colour follows the entity <a class="header-anchor" href="#colour-follows-the-entity" aria-label="Permalink to &quot;Colour follows the entity&quot;">​</a></h2><p>A series takes its palette slot from where it sits in the <code>series</code> array, never from how many of its neighbours are currently visible. Hiding one line leaves the rest exactly the colours they were — a reader who learned that blue is Organic learned something a re-render is not allowed to take back.</p><p>The palette is eight fixed hues with only their lightness following the scheme, measured for colour-vision separation in both. Past eight, fold the tail into one series or draw a second chart; a ninth hue is indistinguishable from one of the first eight whichever one is picked. See <a href="./../../design/design-language">the design language</a> for the measurements.</p><h2 id="the-legend-is-a-control" tabindex="-1">The legend is a control <a class="header-anchor" href="#the-legend-is-a-control" aria-label="Permalink to &quot;The legend is a control&quot;">​</a></h2><p>From two series up it is drawn automatically, and each entry is a real button: clicking one hides its series, hovering one dims the rest. It carries <code>aria-pressed</code>, because a filter that a keyboard cannot reach is a filter half the readers do not have.</p><p>A chart with one series draws no legend — a legend with one swatch in it restates the title.</p><p>A series that is switched off is drawn at Material&#39;s disabled opacity, swatch and name together, because that is what it is and the treatment is one a reader already knows. A series merely standing back while another is hovered is quieter than usual and no more, so the two cannot be mistaken for each other at a glance. Neither depends on hue, which is what keeps them legible in greyscale and in forced colours, and <code>aria-pressed</code> carries the same fact to a reader who is not looking at all.</p><h2 id="markers-and-when-they-stop-helping" tabindex="-1">Markers, and when they stop helping <a class="header-anchor" href="#markers-and-when-they-stop-helping" aria-label="Permalink to &quot;Markers, and when they stop helping&quot;">​</a></h2><p><code>markers=&quot;auto&quot;</code> draws a dot on every join while the dots still have room to be separate marks, and drops them once the line is denser than that. A dot every three pixels is not a row of dots, it is a thicker line.</p><p>The active column keeps its dot whatever the setting says. It is where the crosshair and the panel are pointing, and a crosshair with nothing on it leaves the reader to work out which line it crossed.</p><h2 id="value-labels-are-opt-in-and-selective" tabindex="-1">Value labels are opt-in and selective <a class="header-anchor" href="#value-labels-are-opt-in-and-selective" aria-label="Permalink to &quot;Value labels are opt-in and selective&quot;">​</a></h2><p><code>valueLabels=&quot;last&quot;</code> writes the final number of each series; <code>&quot;extremes&quot;</code> writes the highest and the lowest. <code>&quot;all&quot;</code> exists for the five-point chart where it genuinely is the answer, and it is not the default because a number beside every point is the most reliable way to make a chart unreadable.</p><p>Written values wear ordinary ink, and the swatch beside a name in the legend is the only thing on the chart carrying a series&#39; colour. <code>labelColor=&quot;series&quot;</code> moves the colour onto the words as well, which pairs a long legend with a crowded plot faster than a swatch alone can. It is not the default, and the reason is measurable: the eight palette slots are fitted to 3:1 against the surface, which is the bar a <strong>mark</strong> has to clear, and small text is held to 4.5:1.</p><h2 id="it-brings-no-surface-of-its-own" tabindex="-1">It brings no surface of its own <a class="header-anchor" href="#it-brings-no-surface-of-its-own" aria-label="Permalink to &quot;It brings no surface of its own&quot;">​</a></h2><p>The chart draws a figure, not a card. Wrap it in an <a href="./../layout/box">MPBox</a> or an <a href="./../layout/card">MPCard</a> when it needs one, which keeps a dashboard&#39;s panels the caller&#39;s decision rather than eight components each with an opinion about padding.</p>',50))])}const m=r(c,[["render",p]]);export{y as __pageData,m as default};
