import{_ as a,C as d,o as r,c as i,j as t,a as c,E as s,a4 as n}from"./chunks/framework.CKd2nDfM.js";const M=JSON.parse('{"title":"Admin dashboard","description":"The back office of Grange, a shop that does not exist. A rail, a filter row, four figures, a table with an action on every row and a resizable split under it —…","frontmatter":{"title":"Admin dashboard","order":3,"aside":false},"headers":[],"relativePath":"examples/concept-dashboard.md","filePath":"en/examples/concept-dashboard.md","lastUpdated":1786774258000}'),h={name:"examples/concept-dashboard.md"};function l(m,e,b,p,u,P){const o=d("Demo");return r(),i("div",null,[e[0]||(e[0]=t("h1",{id:"admin-dashboard",tabindex:"-1"},[c("Admin dashboard "),t("a",{class:"header-anchor",href:"#admin-dashboard","aria-label":'Permalink to "Admin dashboard"'},"​")],-1)),e[1]||(e[1]=t("p",{class:"mp-lede"},"The back office of Grange, a shop that does not exist. A rail, a filter row, four figures, a table with an action on every row and a resizable split under it — all on one screen and all at the same size, which is the arrangement that shows whether a size ladder actually holds.",-1)),s(o,{src:"concepts/dashboard",minHeight:960}),e[2]||(e[2]=n('<p>The source is one file: <code>docs/.vitepress/demos/concepts/dashboard.tsx</code>. The table is live — search it, filter it by channel, tick some rows and the bulk actions appear.</p><h2 id="what-it-is-made-of" tabindex="-1">What it is made of <a class="header-anchor" href="#what-it-is-made-of" aria-label="Permalink to &quot;What it is made of&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Block</th><th>Components used</th><th>Worth noticing</th></tr></thead><tbody><tr><td>Rail</td><td><code>MPDrawer</code> <code>MPList</code> <code>MPChip</code> <code>MPCard</code> <code>MPProgressLinear</code></td><td><code>mode=&quot;standard&quot;</code> is the same panel in the layout rather than over it, so a sidebar becoming a hamburger is one prop</td></tr><tr><td>App bar</td><td><code>MPBreadcrumb</code> <code>MPBadge</code> <code>MPTooltip</code> <code>MPIconButton</code> <code>MPAvatar</code></td><td>The rail&#39;s toggle is an icon button with a <code>label</code> that says what it will do, not what it is</td></tr><tr><td>Alert</td><td><code>MPAlert</code></td><td><code>variant=&quot;tonal&quot;</code> with an <code>action</code> — one thing to deal with, and the way to deal with it</td></tr><tr><td>Figures</td><td><code>MPCard</code> <code>MPChip</code> <code>MPIcon</code></td><td>A falling refund rate comes out green: the tile is told which direction is good news, rather than reading the sign</td></tr><tr><td>Filters</td><td><code>MPTextField</code> <code>MPSelect</code> <code>MPDateRangePicker</code></td><td>Three different controls, one height. That is what <code>size=&quot;sm&quot;</code> means across the library</td></tr><tr><td>Bulk actions</td><td><code>MPPill</code> <code>MPButton</code> <code>MPDialog</code> <code>MPSnackbarProvider</code></td><td>They appear only with a selection; the destructive one confirms in a dialog and reports back in a snackbar</td></tr><tr><td>Table</td><td><code>MPTabs</code> <code>MPTable</code> <code>MPCheckbox</code> <code>MPChip</code> <code>MPMenu</code> <code>MPContextMenu</code> <code>MPPagination</code> <code>MPEmpty</code></td><td>Select-all is an <code>indeterminate</code> checkbox in the header cell; every row carries its own menu, and the whole table a context menu</td></tr><tr><td>Split</td><td><code>MPPanes</code> <code>MPPane</code> <code>MPProgressBox</code> <code>MPProgressCircular</code> <code>MPTimeline</code> <code>MPSwitch</code></td><td>The reader decides which half they are reading, so the divider between them is draggable</td></tr><tr><td>New order</td><td><code>MPFloatingActionButton</code></td><td><code>position=&quot;absolute&quot;</code> pins it to this sheet rather than to the window, which is what makes a FAB previewable</td></tr></tbody></table><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><ul><li><code>stickyHeader</code> keeps the column headings in place while the rows scroll under them.</li><li>Every row menu&#39;s trigger carries the order number in its <code>label</code>, so each row action has an accessible name that says which row it belongs to.</li><li>Filtering is ordinary React state. The table renders whatever it is handed and shows <code>MPEmpty</code> when that is nothing — the empty state is a component, not a branch that renders a sentence.</li><li>The columns are built with <code>useMemo</code> inside the component rather than at module scope, because three of the six cells read state: the select-all box, each row&#39;s own box, and the menu that has to know its row.</li></ul>',5))])}const g=a(h,[["render",l]]);export{M as __pageData,g as default};
