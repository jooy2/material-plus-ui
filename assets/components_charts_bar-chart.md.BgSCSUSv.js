import{_ as r,C as t,o as h,c as l,j as a,a as d,E as s,a4 as i}from"./chunks/framework.BoUsC9QI.js";const f=JSON.parse('{"title":"MPBarChart","description":"A measured length per category — how much, next to how much else. Grouped or stacked, upright or on its side, and always from zero.","frontmatter":{"title":"MPBarChart","order":34},"headers":[],"relativePath":"components/charts/bar-chart.md","filePath":"en/components/charts/bar-chart.md","lastUpdated":1789967010000}'),p={name:"components/charts/bar-chart.md"};function c(u,e,g,k,m,b){const o=t("Demo"),n=t("PropsTable");return h(),l("div",null,[e[0]||(e[0]=a("h1",{id:"mpbarchart",tabindex:"-1"},[d("MPBarChart "),a("a",{class:"header-anchor",href:"#mpbarchart","aria-label":'Permalink to "MPBarChart"'},"​")],-1)),e[1]||(e[1]=a("p",{class:"mp-lede"},"A measured length per category — how much, next to how much else. Grouped or stacked, upright or on its side, and always from zero.",-1)),s(o,{src:"bar-chart/hero",minHeight:1420}),e[2]||(e[2]=i(`<div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MPBarChart } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPBarChart</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  categories</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Search&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Direct&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Social&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Mail&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  series</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[{ name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Sessions&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, data: [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4820</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3110</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1940</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">860</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] }]}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;;</span></span></code></pre></div><h2 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-label="Permalink to &quot;Props&quot;">​</a></h2>`,2)),s(n,{name:"MPBarChart"}),e[3]||(e[3]=i('<h2 id="the-axis-starts-at-zero-and-there-is-no-prop-to-stop-it" tabindex="-1">The axis starts at zero and there is no prop to stop it <a class="header-anchor" href="#the-axis-starts-at-zero-and-there-is-no-prop-to-stop-it" aria-label="Permalink to &quot;The axis starts at zero and there is no prop to stop it&quot;">​</a></h2><p>A bar&#39;s length is proportional to its value only from a zero baseline. Crop the axis and a bar twice the height of its neighbour stands for a value five percent larger — which is not a smaller lie for being a common one, and it is told in the shape a reader trusts most.</p><p>A <code>min</code> on <code>yAxis</code> still moves the scale, because a caller who has said so has said so. It is the wrong tool for a set of values that sit close together: the right one is <a href="./line-chart">MPLineChart</a>, whose marks encode position and claim nothing about proportion.</p><h2 id="grouped-or-stacked-never-both" tabindex="-1">Grouped or stacked, never both <a class="header-anchor" href="#grouped-or-stacked-never-both" aria-label="Permalink to &quot;Grouped or stacked, never both&quot;">​</a></h2><p>Side by side compares the parts with each other. Stacked compares the totals and shows what makes them up.</p><p>A stack&#39;s inner segments sit on a wobbly floor and cannot be compared by eye — only the bottom segment and the total have a straight edge to be read against. Reach for it when the total is the point, and for grouping when it is not.</p><p><code>stacked=&quot;percent&quot;</code> fills every bar instead, so the totals leave the picture entirely and what is left is the <strong>composition</strong>. It answers &quot;what is this made of&quot; and refuses to answer &quot;how much of it is there&quot;: a quarter of 1,200 and a quarter of 12 are drawn identically. Put the total somewhere else on the page when a reader needs it — a figure beside the chart, or a second chart.</p><p>A negative has no share of a whole, so a percent stack leaves it out, the way <a href="./pie-chart">MPPieChart</a> does. The table behind the chart still has the number, and the hover panel says both the value and its share.</p><p>Hiding a series with the legend <strong>widens</strong> the survivors rather than leaving a hole in every category, because the band is divided between the series that are drawn.</p><h2 id="rounded-at-the-end-square-at-the-baseline" tabindex="-1">Rounded at the end, square at the baseline <a class="header-anchor" href="#rounded-at-the-end-square-at-the-baseline" aria-label="Permalink to &quot;Rounded at the end, square at the baseline&quot;">​</a></h2><p>The end is where the value is, so that is the end worth softening. A bar rounded where it meets the axis has lost the exact point it starts from, and a row of them turns the baseline into a scalloped edge.</p><p>Which end that is comes from the sign and the orientation together: a negative bar hangs below the axis and a horizontal one grows sideways, and in both the corners to soften are the ones furthest from zero. An inner segment of a stack has no rounded end at all — both of its faces are boundaries between shares rather than the end of anything.</p><h2 id="turn-it-sideways-for-long-names" tabindex="-1">Turn it sideways for long names <a class="header-anchor" href="#turn-it-sideways-for-long-names" aria-label="Permalink to &quot;Turn it sideways for long names&quot;">​</a></h2><p><code>horizontal</code> gives every category a row of its own, which is what a set of names like &quot;Onboarding flow&quot; wants: a name in a row reads left to right at full size, and the bars beside it are still lengths being compared.</p><p>Upright, the axis turns the names instead of cutting them to a slot the width of one bar — 45° while that fits, upright past it. That is the better of the two answers a vertical chart has, and still the worse of the two on this page, because a turned name is read more slowly than a level one. Reach for <code>horizontal</code> when every name is long, and leave <code>xAxis.tickLabels</code> to the chart where one of them is.</p><p><code>xAxis</code> is still the category axis and <code>yAxis</code> is still the value axis when it is turned. The orientation changes the drawing, not what your data means — so a tick format written for values does not suddenly land on the axis holding the names.</p><h2 id="a-long-label-is-turned-not-thrown-away" tabindex="-1">A long label is turned, not thrown away <a class="header-anchor" href="#a-long-label-is-turned-not-thrown-away" aria-label="Permalink to &quot;A long label is turned, not thrown away&quot;">​</a></h2><p>Whichever axis runs along the bottom reads <code>tickLabels</code>, and its default is <code>auto</code>: labels stay level while the widest of them fits its slot, and are turned once it does not. 45° first, because it is the shallower turn and the easier read; upright only where even a turned label&#39;s line box would not fit between two ticks.</p><p>Turning is what the axis reaches for instead of the two things it used to do. Cutting a name to &quot;Onbo…&quot; loses the thing the name was there to say, and showing every second one loses half of them outright — a turned label collides across its <strong>line box</strong> rather than across its own width, and a line box does not grow with the name.</p><p>Short labels are left alone however crowded the axis is. An axis of &quot;Jan&quot;, &quot;Feb&quot;, &quot;Mar&quot; is crowded by how many of them there are and not by how long any one is, and every nth is still the right answer to that.</p><p>So are <strong>ticks</strong>, as opposed to names. A value axis is a ruler: its labels are samples of a continuum, and a reader shown every second one interpolates the rest without noticing. <code>auto</code> therefore turns nothing on the axis along the bottom of a chart that has been turned on its side, where that axis is the values — ask for <code>rotate</code> or <code>vertical</code> by name if you want it anyway.</p><p>The band a turned axis takes is capped at half the chart, and 120px whichever is smaller. Past the cap the names are cut again — a chart whose labels genuinely need more room wants a taller <code>height</code>, or a <code>thickness</code> of its own on the axis. <code>tickLabels=&quot;truncate&quot;</code> goes back to cutting; <code>&quot;rotate&quot;</code> and <code>&quot;vertical&quot;</code> turn every label whether or not it needed it.</p><h2 id="bars-have-a-maximum-thickness" tabindex="-1">Bars have a maximum thickness <a class="header-anchor" href="#bars-have-a-maximum-thickness" aria-label="Permalink to &quot;Bars have a maximum thickness&quot;">​</a></h2><p>Two categories in a wide chart would otherwise be two slabs half the plot across. Past about forty pixels a bar stops reading as a measured length and starts reading as a block of colour, and the axis it is measured against gets no easier to use.</p><h2 id="everything-else-is-the-frame" tabindex="-1">Everything else is the frame <a class="header-anchor" href="#everything-else-is-the-frame" aria-label="Permalink to &quot;Everything else is the frame&quot;">​</a></h2><p>The hover layer, the crosshair, the keyboard walk, the clipped live region and the table behind the picture are the same on every chart here — and the arrow keys follow the category axis, so they run up and down when the chart is turned. <a href="./line-chart">MPLineChart</a> documents them.</p>',26))])}const y=r(p,[["render",c]]);export{f as __pageData,y as default};
