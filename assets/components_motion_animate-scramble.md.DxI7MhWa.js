import{_ as h,C as i,o as l,c as d,j as a,a as t,E as s,a4 as n}from"./chunks/framework.CKd2nDfM.js";const w=JSON.parse(`{"title":"MPAnimateScramble","description":"Text settling out of noise. MPAnimateTyping 's sibling, and the difference is what the box does: this one is its finished length from the first frame.","frontmatter":{"title":"MPAnimateScramble","order":15},"headers":[],"relativePath":"components/motion/animate-scramble.md","filePath":"en/components/motion/animate-scramble.md","lastUpdated":1788333658000}`),c={name:"components/motion/animate-scramble.md"};function p(m,e,f,g,b,u){const r=i("Demo"),o=i("PropsTable");return l(),d("div",null,[e[0]||(e[0]=a("h1",{id:"mpanimatescramble",tabindex:"-1"},[t("MPAnimateScramble "),a("a",{class:"header-anchor",href:"#mpanimatescramble","aria-label":'Permalink to "MPAnimateScramble"'},"​")],-1)),e[1]||(e[1]=a("p",{class:"mp-lede"},[t("Text settling out of noise. "),a("a",{href:"./animate-typing"},"MPAnimateTyping"),t("'s sibling, and the difference is what the box does: this one is its finished length from the first frame.")],-1)),s(r,{src:"animate-scramble/hero",minHeight:360}),e[2]||(e[2]=n(`<div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MPAnimateScramble } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPAnimateScramble</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;Decrypting&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPAnimateScramble</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><h2 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-label="Permalink to &quot;Props&quot;">​</a></h2>`,2)),s(o,{name:"MPAnimateScramble"}),e[3]||(e[3]=n('<h2 id="why-this-rather-than-a-typewriter" tabindex="-1">Why this rather than a typewriter <a class="header-anchor" href="#why-this-rather-than-a-typewriter" aria-label="Permalink to &quot;Why this rather than a typewriter&quot;">​</a></h2><p>A typed line <strong>grows</strong> a character at a time, so everything after it on the page moves while it runs. That is fine for a line on its own in a hero, and it is not fine in a heading with a rule under it, in a table cell, or beside anything at all.</p><p>This one is its finished length from the first frame and only the characters inside it change. Nothing reflows, so it can go where a typewriter cannot.</p><p>The other difference is what the two say. A typewriter says <em>this is being written</em> — there is somebody at the other end. A scramble says <em>this is being resolved</em>, which is what a figure arriving from somewhere, a code being checked or a name being looked up actually is.</p><h2 id="written-in-javascript-and-paused-like-everything-else" tabindex="-1">Written in JavaScript, and paused like everything else <a class="header-anchor" href="#written-in-javascript-and-paused-like-everything-else" aria-label="Permalink to &quot;Written in JavaScript, and paused like everything else&quot;">​</a></h2><p>There is no keyframe for this: replacing a character with a random one is not an interpolation, so the frame loop is real.</p><p>What it borrows is the trigger, the play state and the rewind, so <code>trigger=&quot;visible&quot;</code>, <code>paused</code> and a <code>manual</code> replay behave exactly as they do on the six declared effects. What it does <strong>not</strong> have is a clock of its own that could drift out of step with them.</p><h2 id="waiting-is-noise-and-the-noise-holds-still" tabindex="-1">Waiting is noise, and the noise holds still <a class="header-anchor" href="#waiting-is-noise-and-the-noise-holds-still" aria-label="Permalink to &quot;Waiting is noise, and the noise holds still&quot;">​</a></h2><p>Before it is triggered it shows its own <strong>first frame</strong>, which for this effect is the line fully scrambled — not the answer. A heading that has already resolved while it waits to be scrolled to has given away the thing it was about to do.</p><p>The waiting noise is fixed rather than churning. Motion that starts before the trigger is not the effect; it is a distraction beside it, and a page of paragraphs quietly flickering is a page nobody can read. The characters are drawn from the index and the frame rather than from a <code>Math.random()</code> held in state, so a re-render for any other reason cannot make them jump either.</p><h2 id="examples" tabindex="-1">Examples <a class="header-anchor" href="#examples" aria-label="Permalink to &quot;Examples&quot;">​</a></h2><h3 id="spread" tabindex="-1">spread <a class="header-anchor" href="#spread" aria-label="Permalink to &quot;spread&quot;">​</a></h3><p>How much of the run is spent on the difference between the first character and the last. At <code>0</code> the whole line resolves at once; at <code>1</code> the last character is still noise when the first has been still for the entire run. The default of <code>0.4</code> reads as a wave passing along the line.</p><h3 id="tick" tabindex="-1">tick <a class="header-anchor" href="#tick" aria-label="Permalink to &quot;tick&quot;">​</a></h3><p>How often an unsettled character is replaced. Below about 30ms the noise stops reading as characters and starts reading as flicker.</p><h3 id="characters" tabindex="-1">characters <a class="header-anchor" href="#characters" aria-label="Permalink to &quot;characters&quot;">​</a></h3><p>The alphabet the noise is drawn from. The default is Latin upper case, digits and a few marks: one width apart in most faces and — more to the point — visibly <strong>not words</strong>. A noise alphabet with lower case in it spends half the effect looking like text that has been misspelled rather than like text that has not arrived.</p><h2 id="accessibility" tabindex="-1">Accessibility <a class="header-anchor" href="#accessibility" aria-label="Permalink to &quot;Accessibility&quot;">​</a></h2><ul><li>A screen reader gets the finished line, once, out of a clipped box; the settling copy is <code>aria-hidden</code>. Noise read out character by character is not text, and find-on-page still matches the real words.</li><li>Under <code>prefers-reduced-motion</code> the line is simply there, <strong>finished</strong>, from the first frame — the state rather than the animation. An effect switched off at the wrong end would leave a heading permanently unreadable.</li><li>Characters are counted with <code>Intl.Segmenter</code>, so <code>👩‍👩‍👧</code> is one character of noise rather than seven unrelated glyphs.</li></ul><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./animate-typing">MPAnimateTyping</a> — for a line being written, where the reflow is the point.</li><li><a href="./animate-split">MPAnimateSplit</a> — for a line arriving rather than resolving.</li><li><a href="./animate-counter">MPAnimateCounter</a> — the same idea for a number.</li></ul>',21))])}const y=h(c,[["render",p]]);export{w as __pageData,y as default};
