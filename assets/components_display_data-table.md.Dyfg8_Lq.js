import{_ as h,C as o,o as d,c as l,j as a,a as i,E as t,a4 as n}from"./chunks/framework.CKd2nDfM.js";const y=JSON.parse('{"title":"MPDataTable","description":"A grid of data with the four things a reader does to one: put it in order, cut it down, step through it and take some of it away. Plus a file to keep.","frontmatter":{"title":"MPDataTable","order":29},"headers":[],"relativePath":"components/display/data-table.md","filePath":"en/components/display/data-table.md","lastUpdated":1788593286000}'),c={name:"components/display/data-table.md"};function p(u,e,g,k,w,b){const r=o("Demo"),s=o("PropsTable");return d(),l("div",null,[e[0]||(e[0]=a("h1",{id:"mpdatatable",tabindex:"-1"},[i("MPDataTable "),a("a",{class:"header-anchor",href:"#mpdatatable","aria-label":'Permalink to "MPDataTable"'},"​")],-1)),e[1]||(e[1]=a("p",{class:"mp-lede"},"A grid of data with the four things a reader does to one: put it in order, cut it down, step through it and take some of it away. Plus a file to keep.",-1)),t(r,{src:"data-table/hero",minHeight:520}),e[2]||(e[2]=n(`<div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MPDataTable } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPDataTable</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  headers</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { key: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { key: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;score&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Score&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, align: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;end&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ]}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  items</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{rows}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  getRowKey</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">row</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> row.id}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  sortable</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  searchable</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  paged</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;;</span></span></code></pre></div><h2 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-label="Permalink to &quot;Props&quot;">​</a></h2>`,2)),t(s,{name:"MPDataTable"}),e[3]||(e[3]=a("h3",{id:"mpdatatablecolumn",tabindex:"-1"},[i("MPDataTableColumn "),a("a",{class:"header-anchor",href:"#mpdatatablecolumn","aria-label":'Permalink to "MPDataTableColumn"'},"​")],-1)),t(s,{name:"MPDataTableColumn"}),e[4]||(e[4]=n('<h2 id="why-it-is-not-mptable-with-more-props" tabindex="-1">Why it is not <code>MPTable</code> with more props <a class="header-anchor" href="#why-it-is-not-mptable-with-more-props" aria-label="Permalink to &quot;Why it is not `MPTable` with more props&quot;">​</a></h2><p>Because <a href="./table">MPTable</a> is the presentational half and should stay that way. It draws what it is given, in the order it is given, and a page that only shows a grid has no business shipping a comparator, a CSV writer and a page clamp to do it — <code>MPTable</code> is 3.0 kB and this is not.</p><p>What the two do share is the geometry. <code>internal/table.ts</code> holds the cells&#39; room, the type they are set in and the slot a row&#39;s background is read from, so a data table never sits a pixel off the plain table beside it in the same form.</p><p>It is the same reason <a href="./../inputs/tree-select">MPTreeSelect</a> is not <code>MPSelect</code> with a <code>tree</code> prop: a component that grew a mode for every job it resembles ends up carrying all of them for every caller.</p><h2 id="what-it-does-to-the-rows-and-in-what-order" tabindex="-1">What it does to the rows, and in what order <a class="header-anchor" href="#what-it-does-to-the-rows-and-in-what-order" aria-label="Permalink to &quot;What it does to the rows, and in what order&quot;">​</a></h2><p><strong>Search, then <code>filter</code>, then sort, then the page.</strong> That is the only order in which each step means what it says: sorting before searching would put rows nobody is going to see in order, and paging before either would cut a page out of a list the reader has not asked for yet.</p><p><code>render</code>&#39;s <code>index</code> counts along the sorted, searched order and across every page, so <code>(row, index) =&gt; index + 1</code> is a running row number rather than a position on the screen.</p><h2 id="value-render-compare-exportvalue" tabindex="-1"><code>value</code>, <code>render</code>, <code>compare</code>, <code>exportValue</code> <a class="header-anchor" href="#value-render-compare-exportvalue" aria-label="Permalink to &quot;`value`, `render`, `compare`, `exportValue`&quot;">​</a></h2><p>Four ways to say what a column holds, and the split between them is the whole shape of <code>MPDataTableColumn</code>.</p><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><em>nothing</em></td><td>The cell is <code>row[key]</code>, and that is what is sorted, searched and written</td></tr><tr><td><code>render</code></td><td>What a reader sees</td></tr><tr><td><code>value</code></td><td>What the sort and the search see</td></tr><tr><td><code>compare</code></td><td>How two rows rank, for a value that has no natural order</td></tr><tr><td><code>exportValue</code></td><td>What a file gets</td></tr></tbody></table><p>Most columns need none of them. A column that draws an <a href="./chip">MPChip</a> needs <code>render</code> — and it needs <code>value</code> or <code>compare</code> as well the moment it is sortable, because a React element has no order, and <code>exportValue</code> the moment it is downloadable, because a chip has no text a spreadsheet can hold.</p><p>A <code>compare</code> is always written <strong>ascending</strong>. The table reverses it, so a column that sorted itself would sort itself twice.</p><h2 id="sorting" tabindex="-1">Sorting <a class="header-anchor" href="#sorting" aria-label="Permalink to &quot;Sorting&quot;">​</a></h2><p>Three states per column — ascending, descending, unsorted — because the order the rows arrived in is a state a reader cannot get back to by pressing anything if the cycle only has two. That is the opposite of the choice <a href="./tree-view">MPTreeView</a>&#39;s single select makes, read the other way: there, &quot;nothing chosen&quot; is reachable by pressing something else; here it is not.</p><p>With <code>sortMode=&quot;multiple&quot;</code>, a Shift-press <strong>adds</strong> a column to the sort instead of replacing it, and each sorted heading carries its place in the order. A column added that way stays where it already is in the list, so flipping the second key does not quietly make it the first.</p><p>Two things the default comparison does that a <code>localeCompare</code> does not:</p><ul><li><strong>A run of digits inside a string is read as a number</strong>, so <code>item2</code> comes before <code>item10</code>.</li><li><strong>An empty cell sorts last whichever way the column runs.</strong> A blank is not the smallest value, it is the absence of one, and a descending sort whose first screen is forty blanks has answered a question nobody asked. <code>NaN</code> counts as empty for the same reason: it compares false against everything, so left to the subtraction the order would depend on which rows happened to be next to each other.</li></ul><p>That second rule is why the direction is handed to the comparison rather than applied to what it returns. A sort that reversed the result would reverse the empties with it.</p><h2 id="the-search" tabindex="-1">The search <a class="header-anchor" href="#the-search" aria-label="Permalink to &quot;The search&quot;">​</a></h2><p><code>searchable</code> puts a field above the table. Every column is searched unless it says <code>searchable: false</code>, and what is matched is <code>value</code> — or <code>row[key]</code> — folded with <code>toLocaleLowerCase</code>, which is the fold <a href="./../inputs/combobox">MPCombobox</a> and <a href="./../inputs/command-palette">MPCommandPalette</a> already use.</p><p>A row&#39;s values are folded <strong>once</strong> and joined into one string. The alternative is a fold on every cell of every row on every keystroke, which is what a <code>matches(row, query)</code> signature quietly asks for. They are joined on a character no keyboard produces, so a query cannot span the seam between two cells and find a row on text that is not next to each other — searching <code>adaseoul</code> finds nothing, even in a row holding &quot;Ada&quot; and &quot;Seoul&quot;.</p><p>A <code>Date</code> is deliberately not formatted for the search. What a reader sees in that cell came out of your own <code>render</code>, and guessing a format the search would agree with is how a table ends up not finding a date that is on the screen. Give the column a <code>value</code>.</p><h2 id="the-pages" tabindex="-1">The pages <a class="header-anchor" href="#the-pages" aria-label="Permalink to &quot;The pages&quot;">​</a></h2><p>Every row is rendered otherwise, and there is no windowing — the same ceiling <code>MPTable</code> states, and <code>paged</code> is the answer to it. A few hundred rows is nothing, a few thousand is a visible pause when the table first draws, and past that a table wants either this or a narrower list.</p><p>The page moves back to the first whenever the search changes or the page size does. Left where it was, a search that cut twenty pages to three would leave the reader looking at an empty screen and reading it as &quot;nothing matched&quot;.</p><h2 id="choosing-rows" tabindex="-1">Choosing rows <a class="header-anchor" href="#choosing-rows" aria-label="Permalink to &quot;Choosing rows&quot;">​</a></h2><p><code>selectionMode</code> decides how many, and <code>checkboxes</code> decides how. Single select <strong>toggles</strong> — unlike <a href="./tree-view">MPTreeView</a>&#39;s, because an action bar above a table keys off &quot;nothing chosen&quot; and that has to be reachable. Multiple select takes a Shift-press to extend from the last row pressed, along the order <strong>currently on screen</strong> rather than the order <code>items</code> arrived in.</p><p>The tick in the heading takes the rows on the page, not the whole set: it sits in the header of what is visible, and a control that quietly took four hundred rows the reader cannot see is a control that did something else.</p><p><code>onSelectedChange</code> hands back the keys <strong>and the rows</strong>, including rows the search has hidden and rows on other pages — those are still chosen, so they still have to come back. They arrive in <code>items</code> order rather than the order the presses happened in, because a reader who ticks the last row and then the first has chosen two rows, not an order.</p><h3 id="who-holds-the-keyboard" tabindex="-1">Who holds the keyboard <a class="header-anchor" href="#who-holds-the-keyboard" aria-label="Permalink to &quot;Who holds the keyboard&quot;">​</a></h3><p>A row that answers a press has to answer a keyboard, and a table of two hundred rows that each did would be two hundred tab stops. So there is exactly one route into a row, and <code>checkboxes</code> picks it:</p><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><code>checkboxes</code></td><td>The tick is the control; the row is not in the tab order</td></tr><tr><td><code>selectionMode</code> without ticks</td><td>The row is</td></tr><tr><td><code>onRowClick</code> alone</td><td>The row is — there is nothing else to press</td></tr></tbody></table><h2 id="resizing" tabindex="-1">Resizing <a class="header-anchor" href="#resizing" aria-label="Permalink to &quot;Resizing&quot;">​</a></h2><p><code>resizable</code> gives every heading a handle, and the handle is a <strong>control</strong> rather than a target: it takes focus, and the arrow keys widen and narrow the column by 8px, or 32px with Shift. Home gives the column its original width back, and so does a double-click. Column width is a real preference — reading a long value — and there is no other way to get it.</p><p><strong>It also changes how the columns are laid out, and it has to.</strong> Without it the browser balances the columns against their contents, which is what makes a plain table read well; but a balanced column springs back from a drag, because a width in that layout is a hint rather than a measurement. Turning it on fixes the layout, so <code>width</code> becomes exact and a column that has not said how wide it is takes 160.</p><h2 id="the-download" tabindex="-1">The download <a class="header-anchor" href="#the-download" aria-label="Permalink to &quot;The download&quot;">​</a></h2><p><code>exportable</code> adds a button that writes the rows out as a CSV file. <strong>Every row the reader is currently looking at, not the page they are on</strong>: the search and the sort are applied and the paging is not, because a file of page three is not a file anybody asked for.</p><p>The escaping is RFC 4180 and it is four rules — a field is quoted if it holds the separator, a quote or a line break, and a quote inside a quoted field is written twice. Getting it wrong is silent: a comma inside a cell shifts every column after it by one, and nobody notices until a spreadsheet somewhere is off by a column.</p><p>The file leads with a byte-order mark, and not as a nicety: Excel reads a UTF-8 CSV without one as the local code page, so every non-ASCII name in it arrives as mojibake. Every other reader ignores the mark.</p><p><code>onExport</code> takes the text instead of downloading it — to post it somewhere, to open it in a viewer of your own, or to put a sheet around it.</p><h2 id="what-it-deliberately-does-not-do" tabindex="-1">What it deliberately does not do <a class="header-anchor" href="#what-it-deliberately-does-not-do" aria-label="Permalink to &quot;What it deliberately does not do&quot;">​</a></h2><p>Virtual scrolling, cell editing, grouped rows with aggregates, column reordering, frozen columns, and a mode where the server does the sorting. Each of those is a component&#39;s worth of decisions rather than a prop, and a table that half-did six of them would be worse than one that does five things completely. <code>paged</code> is the answer to the row count; the rest is a table this library has not built yet.</p>',42))])}const f=h(c,[["render",p]]);export{y as __pageData,f as default};
