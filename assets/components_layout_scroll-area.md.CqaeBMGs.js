import{_ as r,C as t,o as h,c as l,j as a,a as d,E as s,a4 as i}from"./chunks/framework.CKd2nDfM.js";const f=JSON.parse(`{"title":"MPScrollArea","description":"A box with a scrollbar of its own. The browser's is drawn by the operating system — a different width on every machine and a different colour from the sheet it…","frontmatter":{"title":"MPScrollArea","order":22},"headers":[],"relativePath":"components/layout/scroll-area.md","filePath":"en/components/layout/scroll-area.md","lastUpdated":1788503782000}`),c={name:"components/layout/scroll-area.md"};function p(b,e,k,m,g,u){const o=t("Demo"),n=t("PropsTable");return h(),l("div",null,[e[0]||(e[0]=a("h1",{id:"mpscrollarea",tabindex:"-1"},[d("MPScrollArea "),a("a",{class:"header-anchor",href:"#mpscrollarea","aria-label":'Permalink to "MPScrollArea"'},"​")],-1)),e[1]||(e[1]=a("p",{class:"mp-lede"},"A box with a scrollbar of its own. The browser's is drawn by the operating system — a different width on every machine and a different colour from the sheet it is cut into; this one is an element, so it is the same everywhere and made of the library's own tokens.",-1)),s(o,{src:"scroll-area/hero",minHeight:300}),e[2]||(e[2]=i(`<div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MPScrollArea } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPScrollArea</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> maxHeight</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">320</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPList</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;…&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPList</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPScrollArea</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><h2 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-label="Permalink to &quot;Props&quot;">​</a></h2>`,2)),s(n,{name:"MPScrollArea"}),e[3]||(e[3]=i('<h2 id="something-has-to-bound-the-box" tabindex="-1">Something has to bound the box <a class="header-anchor" href="#something-has-to-bound-the-box" aria-label="Permalink to &quot;Something has to bound the box&quot;">​</a></h2><p>A scroll area whose height is its content&#39;s height never overflows and never shows a bar. <code>maxHeight</code> is the usual way to say what bounds it; a <code>className</code> carrying a <code>max-h-*</code>, or a flex parent that constrains it, are the others.</p><p>The bound lands on the <strong>viewport</strong> rather than on the outer element, which matters if you are reaching in with a selector: a <code>max-height</code> on the root would leave its own height <code>auto</code>, and a viewport asking for <code>height: 100%</code> of an <code>auto</code> parent gets its content&#39;s height instead — a box that is exactly as tall as what is in it and never scrolls.</p><h2 id="it-is-still-a-real-scroll-container" tabindex="-1">It is still a real scroll container <a class="header-anchor" href="#it-is-still-a-real-scroll-container" aria-label="Permalink to &quot;It is still a real scroll container&quot;">​</a></h2><p>The native bar is hidden rather than the scrolling being reimplemented, which is the difference between this and a component that listens for <code>wheel</code> events. The wheel, the trackpad, a touch drag, Page Up, Home and End, a keyboard focus moving into something below the fold, the browser&#39;s own scroll anchoring and its overscroll behaviour all work exactly as they did.</p><h2 id="the-bar-overlays-the-content" tabindex="-1">The bar overlays the content <a class="header-anchor" href="#the-bar-overlays-the-content" aria-label="Permalink to &quot;The bar overlays the content&quot;">​</a></h2><p>It takes no room in the layout, which is what makes this swappable for the browser&#39;s own on a page that has already been designed: adding one reflows nothing.</p><p>The trade is that content can pass underneath it, so a box whose content reaches the edge wants a little inline padding of its own.</p><h2 id="there-is-no-bar-until-there-is-something-to-scroll" tabindex="-1">There is no bar until there is something to scroll <a class="header-anchor" href="#there-is-no-bar-until-there-is-something-to-scroll" aria-label="Permalink to &quot;There is no bar until there is something to scroll&quot;">​</a></h2><p>The overflow is measured after the first render, and a viewport that does not overflow has no scrollbar in the DOM at all. That is why <code>persistent</code> is about <strong>opacity</strong> rather than about mounting: it holds a bar that exists, and does not conjure one for a box that fits.</p><p>Left off, the bar behaves the way an overlay scrollbar does — drawn while scrolling or hovered, faded out otherwise. Turn it on for a panel whose scrollability is not obvious from its content, where a bar that appears only on hover is a bar a reader has to discover.</p><h2 id="axis" tabindex="-1"><code>axis</code> <a class="header-anchor" href="#axis" aria-label="Permalink to &quot;`axis`&quot;">​</a></h2><p><code>vertical</code> is the default and the common case. <code>both</code> is for content with a width of its own — a wide table, a diagram — where neither axis should be the one that wraps, and it is the only case that draws the little square where two bars meet.</p><h2 id="size-is-the-bar-s-thickness" tabindex="-1"><code>size</code> is the bar&#39;s thickness <a class="header-anchor" href="#size-is-the-bar-s-thickness" aria-label="Permalink to &quot;`size` is the bar&#39;s thickness&quot;">​</a></h2><p>The only thing <code>size</code> sets here. A scrollbar is not a control and has no height on the ladder; what it has is a width, and the five rungs run 6 to 14 pixels. <code>md</code> is 10, close to what macOS draws and much narrower than Windows&#39; classic 17 — the point is not to match either, but to be the same on both.</p>',15))])}const y=r(c,[["render",p]]);export{f as __pageData,y as default};
