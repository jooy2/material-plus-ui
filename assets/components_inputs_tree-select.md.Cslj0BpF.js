import{_ as h,C as a,o as r,c as l,j as s,a as p,E as t,a4 as i}from"./chunks/framework.CKd2nDfM.js";const w=JSON.parse('{"title":"MPTreeSelect","description":"A value chosen from a tree rather than from a list. The gap between a select and a tree view: a hierarchy that folds away behind a field.","frontmatter":{"title":"MPTreeSelect","order":17},"headers":[],"relativePath":"components/inputs/tree-select.md","filePath":"en/components/inputs/tree-select.md","lastUpdated":1788569639000}'),d={name:"components/inputs/tree-select.md"};function c(k,e,u,g,m,E){const n=a("Demo"),o=a("PropsTable");return r(),l("div",null,[e[0]||(e[0]=s("h1",{id:"mptreeselect",tabindex:"-1"},[p("MPTreeSelect "),s("a",{class:"header-anchor",href:"#mptreeselect","aria-label":'Permalink to "MPTreeSelect"'},"​")],-1)),e[1]||(e[1]=s("p",{class:"mp-lede"},"A value chosen from a tree rather than from a list. The gap between a select and a tree view: a hierarchy that folds away behind a field.",-1)),t(n,{src:"tree-select/hero",minHeight:420}),e[2]||(e[2]=i(`<div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MPTreeSelect } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPTreeSelect</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  label</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Region&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  searchable</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  items</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{[</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      value: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;asia&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Asia&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      children: [{ value: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;seoul&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, label: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Seoul&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ]}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;;</span></span></code></pre></div><h2 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-label="Permalink to &quot;Props&quot;">​</a></h2>`,2)),t(o,{name:"MPTreeSelect"}),e[3]||(e[3]=i('<h2 id="why-not-a-select-with-indented-options" tabindex="-1">Why not a select with indented options <a class="header-anchor" href="#why-not-a-select-with-indented-options" aria-label="Permalink to &quot;Why not a select with indented options&quot;">​</a></h2><p>Because indentation in a flat list is a picture of a hierarchy rather than the thing itself. Nothing folds, so a taxonomy of two hundred nodes arrives two hundred rows long; nothing says which rows belong to the one above them once you have scrolled past it; and there is no way to say that a row groups the rows under it without being one of them.</p><p><a href="./select">MPSelect</a> is the right control for a list of answers. This is the one for answers that live somewhere.</p><h2 id="branches-are-not-answers" tabindex="-1">Branches are not answers <a class="header-anchor" href="#branches-are-not-answers" aria-label="Permalink to &quot;Branches are not answers&quot;">​</a></h2><p><code>selectableBranches</code> is off by default, because that is the shape most of these trees have: the branches are the taxonomy and the leaves are the answers. A &quot;Europe&quot; that can be chosen alongside &quot;France&quot; is usually a data model nobody meant — and the day it <em>is</em> meant, turning one prop on says so.</p><p>An item&#39;s own <code>selectable</code> overrules the rule either way, which is how a tree with exactly one selectable branch states it.</p><p>A branch that cannot be chosen is <strong>not a disabled row</strong>. It looks like every other row, it takes focus, and it opens and shuts — a taxonomy whose branches do not open is a list with extra steps. What it does not do is answer: its press is intercepted before the tree sees it, so pressing &quot;Europe&quot; opens Europe and leaves the &quot;France&quot; you had chosen exactly where it was.</p><h2 id="a-search-keeps-the-ancestors" tabindex="-1">A search keeps the ancestors <a class="header-anchor" href="#a-search-keeps-the-ancestors" aria-label="Permalink to &quot;A search keeps the ancestors&quot;">​</a></h2><p><code>searchable</code> puts a field above the tree. What it filters to is every match <strong>and every ancestor of one</strong> — filtered to bare matches, a tree is a list, and a list of leaves is exactly what a tree was chosen over. &quot;Seoul&quot; under nothing at all does not say which Seoul, or which taxonomy it came from.</p><p>Two rules follow from the same reasoning:</p><ul><li><strong>A node that matches keeps all of its children</strong>, not only the ones that matched. Having found the branch the reader asked for, hiding what is inside it is the opposite of helpful.</li><li><strong>Every branch the filter kept is opened.</strong> A match folded inside a shut parent is a match the reader was not shown.</li></ul><p>While a search is on, the tree&#39;s open branches are the search&#39;s answer rather than the reader&#39;s, so <code>onExpandedChange</code> is quiet. Clearing the field — or shutting the popup, which clears it — gives the reader&#39;s own back.</p><p>Matching is <code>toLocaleLowerCase</code> and nothing more, which is the fold <a href="./combobox">MPCombobox</a> already uses. <code>searchLabel</code> is what a node is matched against when its <code>label</code> is not a plain string.</p><p>The field&#39;s own word is <a href="./transfer">MPTransfer</a>&#39;s &quot;Search&quot;, which is the same field doing the same job — one more spelling of it would be one more thing to translate in eighteen locales, and one more chance for the two to disagree.</p><h2 id="what-the-trigger-writes" tabindex="-1">What the trigger writes <a class="header-anchor" href="#what-the-trigger-writes" aria-label="Permalink to &quot;What the trigger writes&quot;">​</a></h2><p>The chosen labels, comma-joined. <code>format</code> replaces that with anything else, and a multi-select of a hundred nodes is the case it exists for — <code>format={(chosen) =&gt; `${chosen.length} chosen`}</code> says more in less room than the ninety-nine labels that would not have fitted.</p><h2 id="name" tabindex="-1"><code>name</code> <a class="header-anchor" href="#name" aria-label="Permalink to &quot;`name`&quot;">​</a></h2><p>One <code>&lt;input type=&quot;hidden&quot;&gt;</code> per value, all under the same name, so a form reads a multi-select as a repeated field. That is the shape <code>FormData.getAll</code> expects and the one a select with <code>multiple</code> already submits.</p>',18))])}const y=h(d,[["render",c]]);export{w as __pageData,y as default};
