import{_ as d,C as a,o as n,c,j as t,a as i,E as r,a4 as s}from"./chunks/framework.CKd2nDfM.js";const f=JSON.parse('{"title":"Landing page","description":"A marketing page for Kestrel, a deploy tool that does not exist. It is the screen a component library is least obviously for — mostly type, space and one call…","frontmatter":{"title":"Landing page","order":2,"aside":false},"headers":[],"relativePath":"examples/concept-landing.md","filePath":"en/examples/concept-landing.md","lastUpdated":1786774258000}'),l={name:"examples/concept-landing.md"};function h(p,e,m,u,g,P){const o=a("Demo");return n(),c("div",null,[e[0]||(e[0]=t("h1",{id:"landing-page",tabindex:"-1"},[i("Landing page "),t("a",{class:"header-anchor",href:"#landing-page","aria-label":'Permalink to "Landing page"'},"​")],-1)),e[1]||(e[1]=t("p",{class:"mp-lede"},"A marketing page for Kestrel, a deploy tool that does not exist. It is the screen a component library is least obviously for — mostly type, space and one call to action repeated — which makes it the best test of whether the parts compose.",-1)),r(o,{src:"concepts/landing",minHeight:2600}),e[2]||(e[2]=s('<p>The source is one file: <code>docs/.vitepress/demos/concepts/landing.tsx</code>. Everything on the page is a Material Plus component or a Material Plus token; nothing is a bare <code>div</code> dressed up as one.</p><h2 id="what-it-is-made-of" tabindex="-1">What it is made of <a class="header-anchor" href="#what-it-is-made-of" aria-label="Permalink to &quot;What it is made of&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Block</th><th>Components used</th><th>Worth noticing</th></tr></thead><tbody><tr><td>Announcement</td><td><code>MPPill</code></td><td>A pill with <code>onClick</code> is a button, so the banner is reachable from the keyboard with no extra markup around it</td></tr><tr><td>Header</td><td><code>MPIcon</code> <code>MPButton</code> <code>MPIconButton</code> <code>MPTooltip</code></td><td>The nav links are <code>variant=&quot;text&quot;</code> buttons, which is what keeps them on one baseline with the solid one beside them</td></tr><tr><td>Hero</td><td><code>MPTypography</code> <code>MPChip</code> <code>MPButton</code> <code>MPAvatar</code></td><td><code>MPTypography</code> sets the element as well as the scale, so <code>level=&quot;h1&quot;</code> is a real <code>&lt;h1&gt;</code></td></tr><tr><td>Trust strip</td><td><code>MPDivider</code></td><td>A divider with children carries the section label, so the rule and the heading over it are one element</td></tr><tr><td>Numbers</td><td><code>MPGrid</code> <code>MPGridItem</code></td><td>Two across on a phone, four from the medium window up — each item&#39;s <code>span</code> is <code>{ compact: 6, medium: 3 }</code>, and nothing else</td></tr><tr><td>Features</td><td><code>MPCard</code> <code>MPIcon</code></td><td>The glyph sits in <code>headerAction</code>, which keeps it on the title&#39;s baseline at every size</td></tr><tr><td>Product tour</td><td><code>MPTabs</code> <code>MPProgressLinear</code> <code>MPList</code> <code>MPChip</code></td><td>Three views of the same product; the funnel is <code>showValue</code> on a bar rather than a chart</td></tr><tr><td>Quote</td><td><code>MPBlockquote</code></td><td><code>author</code> and <code>source</code> are separate slots, so the attribution wraps as two lines on a narrow screen</td></tr><tr><td>Pricing</td><td><code>MPSegmentedButton</code> <code>MPCard</code> <code>MPList</code> <code>MPButton</code> <code>MPTable</code></td><td>The billing toggle switches both prices at once; the comparison table is rendered from a column list</td></tr><tr><td>Questions</td><td><code>MPAccordion</code></td><td>One <code>MPAccordionItem</code> per question, all closed on arrival</td></tr><tr><td>Closing form</td><td><code>MPCard</code> <code>MPTextField</code> <code>MPButton</code></td><td>The only field on the page, inside a real <code>&lt;form&gt;</code> with its own submit</td></tr><tr><td>Footer</td><td><code>MPDivider</code> <code>MPTextLink</code></td><td><code>MPTextLink</code> is the one link that keeps a colour and an underline — every other link in a component takes the component&#39;s own type</td></tr></tbody></table><h2 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h2><ul><li>Colour carries meaning rather than emphasis. The featured plan is the only <code>elevated</code> card in the pricing row and the only <code>primary</code> button; the other two stay <code>outlined</code> and <code>secondary</code>.</li><li>The email field validates on change but only shows <code>errorMessage</code> once something has been typed, so an untouched form is never red.</li><li>Nothing in the page sets a width in pixels except the grid gutters. The columns are <code>repeat(auto-fit, minmax(min(100%, …), 1fr))</code>, so the layout follows the space it is given rather than the window it is in.</li></ul>',5))])}const M=d(l,[["render",h]]);export{f as __pageData,M as default};
