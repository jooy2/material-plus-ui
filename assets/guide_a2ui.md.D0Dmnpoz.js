import{_ as e,o as t,c as s,a4 as i}from"./chunks/framework.BoUsC9QI.js";const k=JSON.parse('{"title":"A2UI catalog","description":"An agent describes an interface, and Material Plus draws it. A2UI is a protocol for exactly that: the agent sends JSON naming components from a catalog, a…","frontmatter":{"title":"A2UI catalog","order":4},"headers":[],"relativePath":"guide/a2ui.md","filePath":"en/guide/a2ui.md","lastUpdated":1789785470000}'),n={name:"guide/a2ui.md"};function o(d,a,h,r,l,c){return t(),s("div",null,[...a[0]||(a[0]=[i(`<h1 id="a2ui-catalog" tabindex="-1">A2UI catalog <a class="header-anchor" href="#a2ui-catalog" aria-label="Permalink to &quot;A2UI catalog&quot;">​</a></h1><p class="mp-lede">An agent describes an interface, and Material Plus draws it. <a href="https://a2ui.org">A2UI</a> is a protocol for exactly that: the agent sends JSON naming components from a <em>catalog</em>, a renderer resolves it on the client, and <code>material-plus-ui/a2ui</code> is that catalog implemented as Material Design 3.</p><p>The point of a catalog is that the agent never writes markup. It writes <code>{ &quot;component&quot;: &quot;Button&quot;, &quot;variant&quot;: &quot;primary&quot; }</code>, and what that draws is the client&#39;s decision — so the same agent renders as Material here, and as something else in an application that registered a different catalog. Nothing in the payload can reach past the vocabulary the catalog declares.</p><p>There are two catalogs here. <code>mpA2uiCatalog</code> implements A2UI&#39;s own <strong>basic catalog</strong>, all eighteen components, under the basic catalog&#39;s own id — so an agent already targeting it needs no change. <code>mpA2uiExtendedCatalog</code> is those eighteen plus five of this library&#39;s own, under an id of ours, for the surfaces the basic vocabulary cannot express: a table, three charts and a figure.</p><h2 id="install" tabindex="-1">Install <a class="header-anchor" href="#install" aria-label="Permalink to &quot;Install&quot;">​</a></h2><p>The protocol&#39;s SDK is two packages, and this library declares both as optional peer dependencies — install them alongside it.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> material-plus-ui</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @a2ui/react</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @a2ui/web_core</span></span></code></pre></div><p>They are optional because they are substantial: Lit, signals, Zod, a date library and a Markdown parser arrive with them, against the two runtime dependencies the rest of this package has. Nothing outside this subpath imports either one, so a project that renders no agent interfaces downloads none of it.</p><p>What it costs a project that does is measured in the build, beside every other figure in this documentation:</p><table tabindex="0"><thead><tr><th>Bundle</th><th>This library</th><th>With the SDK</th></tr></thead><tbody><tr><td><code>mpA2uiCatalog</code></td><td>38.9 kB</td><td>77.3 kB</td></tr><tr><td><code>mpA2uiExtendedCatalog</code></td><td>60.6 kB</td><td>98.9 kB</td></tr></tbody></table><p>Gzipped, with React and Base UI external. The difference between the two rows is the data table and the three charts, which is why the basic catalog is its own export rather than a subset of the other: import the one you register and the rest is dropped.</p><h2 id="render-a-surface" tabindex="-1">Render a surface <a class="header-anchor" href="#render-a-surface" aria-label="Permalink to &quot;Render a surface&quot;">​</a></h2><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { A2uiSurface } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@a2ui/react/v0_9&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MessageProcessor } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@a2ui/web_core/v0_9&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { mpA2uiCatalog } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui/a2ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui/styles.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> processor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MessageProcessor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([mpA2uiCatalog], (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">action</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // What the reader did, on its way back to the agent.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(action);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">processor.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">processMessages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(messagesFromTheAgent);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Surfaces</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> surfaces</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Array.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(processor.model.surfacesMap.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">values</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> surfaces.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">surface</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">A2uiSurface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> key</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{surface.id} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">surface</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{surface} /&gt;);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Three things are worth naming in that snippet. <code>mpA2uiCatalog</code> is what decides how the payload is drawn. The second argument to <code>MessageProcessor</code> is where a press, a keystroke or a choice leaves for the agent. And <code>material-plus-ui/styles.css</code> is the only stylesheet involved — the catalog adds no CSS of its own, because it draws with components this library already styles.</p><p>The catalog id the agent has to send is exported, so it does not have to be retyped on either side:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { A2UI_BASIC_CATALOG_ID } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui/a2ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;https://a2ui.org/specification/v0_9/catalogs/basic/catalog.json&#39;</span></span></code></pre></div><p>A client announces the same string in <code>supportedCatalogIds</code>, and <code>processor.getClientCapabilities()</code> produces that for you.</p><h2 id="what-each-component-draws" tabindex="-1">What each component draws <a class="header-anchor" href="#what-each-component-draws" aria-label="Permalink to &quot;What each component draws&quot;">​</a></h2><table tabindex="0"><thead><tr><th>A2UI</th><th>Drawn as</th><th>Notes</th></tr></thead><tbody><tr><td><code>Text</code></td><td><code>MPTypography</code></td><td><code>h1</code>–<code>h5</code> are the type scale&#39;s headings, <code>caption</code> its small print</td></tr><tr><td><code>Image</code></td><td><code>MPImage</code></td><td><code>variant</code> is a box; a loading and an error state come with it</td></tr><tr><td><code>Icon</code></td><td><code>MPIcon</code></td><td>The protocol&#39;s 59 names, mapped to lucide glyphs</td></tr><tr><td><code>Video</code></td><td><code>&lt;video controls&gt;</code></td><td>The browser&#39;s player, with Material&#39;s corner</td></tr><tr><td><code>AudioPlayer</code></td><td><code>&lt;audio controls&gt;</code></td><td>The same, with the description drawn above it</td></tr><tr><td><code>Row</code>, <code>Column</code></td><td><code>MPFlex</code></td><td>An 8px gap, which the agent cannot set</td></tr><tr><td><code>List</code></td><td><code>MPFlex</code> as <code>&lt;ul&gt;</code> / <code>&lt;ol&gt;</code></td><td>A real list element, and <code>&lt;li&gt;</code> per child</td></tr><tr><td><code>Card</code></td><td><code>MPCard</code></td><td>One child, on a sheet</td></tr><tr><td><code>Tabs</code></td><td><code>MPTabs</code></td><td>Keyed by child id, so a streamed payload can add a tab</td></tr><tr><td><code>Modal</code></td><td><code>MPDialog</code></td><td>Scrim, focus trap, Escape, focus restored</td></tr><tr><td><code>Divider</code></td><td><code>MPDivider</code></td><td>Both axes</td></tr><tr><td><code>Button</code></td><td><code>MPButton</code></td><td><code>primary</code> filled, <code>default</code> outlined, <code>borderless</code> text</td></tr><tr><td><code>TextField</code></td><td><code>MPTextField</code>, <code>MPNumberField</code></td><td><code>number</code> is the numeric field, with steppers</td></tr><tr><td><code>CheckBox</code></td><td><code>MPCheckbox</code></td><td>Label tied to the box</td></tr><tr><td><code>ChoicePicker</code></td><td><code>MPCombobox</code>, <code>MPRadioGroup</code>, <code>MPCheckbox</code>, <code>MPChip</code></td><td>See below</td></tr><tr><td><code>Slider</code></td><td><code>MPSlider</code></td><td>The value is always shown</td></tr><tr><td><code>DateTimeInput</code></td><td><code>MPDatePicker</code>, <code>MPTimePicker</code>, <code>MPDateTimePicker</code></td><td>Chosen by the two switches</td></tr></tbody></table><p>Every component also carries the protocol&#39;s two shared props. <code>weight</code> becomes a flex share inside a <code>Row</code> or a <code>Column</code>, and <code>accessibility</code> becomes the element&#39;s accessible name and its <code>title</code> — except on a labelled control, where the payload&#39;s own <code>label</code> is what the reader sees and an <code>aria-label</code> beside it would replace the visible label with an invisible one.</p><h3 id="choicepicker-is-four-controls" tabindex="-1">ChoicePicker is four controls <a class="header-anchor" href="#choicepicker-is-four-controls" aria-label="Permalink to &quot;ChoicePicker is four controls&quot;">​</a></h3><p>The protocol describes one component along three axes — one choice or several, boxes or chips, filterable or not — and Material has a different control for most of the combinations:</p><ul><li><strong>Filterable</strong> is <code>MPCombobox</code>, single or multiple. Filtering is a control of its own in Material rather than a text box above a list: the field belongs to the list, it announces its matches, its empty state is already translated, and a long list does not have to be on the page to be searched.</li><li><strong>One choice, boxes</strong> is <code>MPRadioGroup</code>, which is what &quot;exactly one of these&quot; means to a screen reader, and brings arrow-key movement with it.</li><li><strong>Anything else</strong> is an <code>MPFieldset</code> of checkboxes, or of chips when the agent asked for chips. The legend is what ties the group together.</li></ul><h3 id="dates-keep-their-day" tabindex="-1">Dates keep their day <a class="header-anchor" href="#dates-keep-their-day" aria-label="Permalink to &quot;Dates keep their day&quot;">​</a></h3><p><code>DateTimeInput</code> carries ISO 8601 strings and every picker here works in <code>Date</code>, so the two are converted at that boundary — by hand, rather than through <code>new Date(value)</code>. The reason is the shape an agent sends most: <code>new Date(&#39;2026-03-01&#39;)</code> is midnight in Greenwich, which is the 28th of February anywhere west of it, and a calendar that opens on the wrong day is a bug nobody can explain. The value is written back at the precision that was asked for, as a local wall clock, so reading it and writing it never moves it.</p><h3 id="markdown-is-off-until-you-turn-it-on" tabindex="-1">Markdown is off until you turn it on <a class="header-anchor" href="#markdown-is-off-until-you-turn-it-on" aria-label="Permalink to &quot;Markdown is off until you turn it on&quot;">​</a></h3><p>The specification says <code>body</code> text may carry simple Markdown. Rendering Markdown means turning a string an agent wrote into HTML, and this library ships no parser and no sanitizer. So the protocol&#39;s own arrangement is used: configure a renderer through <code>@a2ui/react</code>&#39;s Markdown context — <code>@a2ui/markdown-it</code>, or your own — and formatted text renders. With none configured, an agent&#39;s string is drawn as the characters it contains, <code>**like this**</code>.</p><p>That is the safe default rather than the tidy one. A renderer&#39;s contract is that it must sanitize what it returns, and until there is one to hold to that, nothing is treated as HTML.</p><h2 id="what-the-agent-can-and-cannot-do" tabindex="-1">What the agent can and cannot do <a class="header-anchor" href="#what-the-agent-can-and-cannot-do" aria-label="Permalink to &quot;What the agent can and cannot do&quot;">​</a></h2><p>A surface is JSON from a language model, so the boundary is worth stating plainly.</p><ul><li><strong>The catalog is the allowlist.</strong> A component name the registered catalog does not hold never resolves, and no payload can reach a component of yours unless you put it there.</li><li><strong>Props are validated against the schemas</strong> by the SDK&#39;s binder before anything is drawn, so a malformed surface is refused rather than rendered halfway.</li><li><strong>Text is text</strong>, unless you configured a Markdown renderer, as above.</li><li><strong>URLs are the agent&#39;s.</strong> <code>Image</code>, <code>Video</code> and <code>AudioPlayer</code> load what the payload names, so a surface from an agent you do not control belongs behind a Content Security Policy that says where media may come from.</li></ul><h2 id="this-library-s-own-catalog" tabindex="-1">This library&#39;s own catalog <a class="header-anchor" href="#this-library-s-own-catalog" aria-label="Permalink to &quot;This library&#39;s own catalog&quot;">​</a></h2><p>Five components the basic vocabulary has no way to describe, which is what an agent reporting numbers needs first:</p><table tabindex="0"><thead><tr><th>Component</th><th>Drawn as</th><th>For</th></tr></thead><tbody><tr><td><code>DataTable</code></td><td><code>MPDataTable</code></td><td>Rows and columns, with sorting, searching, paging and selection</td></tr><tr><td><code>BarChart</code></td><td><code>MPBarChart</code></td><td>A value across categories, stacked or side by side</td></tr><tr><td><code>LineChart</code></td><td><code>MPLineChart</code></td><td>A value over a sequence, with a curve that is a claim about the data</td></tr><tr><td><code>PieChart</code></td><td><code>MPPieChart</code></td><td>Parts of one whole, as a pie, a donut or a half</td></tr><tr><td><code>Statistic</code></td><td><code>MPStatistic</code></td><td>One figure, with what it is and how it has changed</td></tr></tbody></table><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { mpA2uiCatalog, mpA2uiExtendedCatalog } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui/a2ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> processor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MessageProcessor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([mpA2uiCatalog, mpA2uiExtendedCatalog]);</span></span></code></pre></div><p>Register both and the agent chooses per surface: the basic id for anything another renderer might also have to draw, and <code>MP_A2UI_CATALOG_ID</code> when the surface is a table or a chart. Its schema is published at that id — <a href="https://material-plus.cdget.com/a2ui/v0_9/catalog.json" target="_blank" rel="noreferrer">material-plus.cdget.com/a2ui/v0_9/catalog.json</a> — generated from the same Zod schemas the renderer enforces, so the description an agent reads and the contract a payload is checked against cannot drift apart.</p><h3 id="the-data-lives-in-the-data-model" tabindex="-1">The data lives in the data model <a class="header-anchor" href="#the-data-lives-in-the-data-model" aria-label="Permalink to &quot;The data lives in the data model&quot;">​</a></h3><p>A table&#39;s <code>rows</code> and a chart&#39;s <code>series</code> take a literal array or a path, like every other value in the protocol:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sales&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;component&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;BarChart&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;series&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/monthly&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;categories&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Jan&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Feb&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Mar&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;label&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Orders by month&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Bound to a path, the numbers arrive — and keep arriving — without the chart being resent. That is also the case worth knowing about: a path is resolved rather than validated. The schema checked the <em>binding</em>, and what comes back is whatever the data model holds, which may be half-written or not what the agent intended. So a value that is not a number is a gap, a series that is not a list is an empty chart, and a cell holding an object is written out as JSON rather than taking the surface down.</p><h3 id="what-is-deliberately-not-there" tabindex="-1">What is deliberately not there <a class="header-anchor" href="#what-is-deliberately-not-there" aria-label="Permalink to &quot;What is deliberately not there&quot;">​</a></h3><p>No per-row action. An A2UI action carries a context resolved from data paths when it fires, which cannot say <em>which</em> row was pressed — a row callback would be one that could not report what it happened to. Selection says it instead: bind <code>selectedKeys</code>, and the reader&#39;s choice is in the data model for the next message to read.</p><p>No <code>Intl</code> options, no cell renderers, no column accessors. Those are the props that make these components flexible in a page and they are functions, so an agent has no way to send one. What replaces them is a narrower question — <code>align</code>, <code>compact</code>, <code>curve</code> — that an agent can answer from JSON.</p><h2 id="your-own-catalog" tabindex="-1">Your own catalog <a class="header-anchor" href="#your-own-catalog" aria-label="Permalink to &quot;Your own catalog&quot;">​</a></h2><p>A project that wants its own vocabulary — its own components, its own names — needs an id of its own, because that is a different contract. <code>createMPA2uiCatalog</code> is where that starts:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createMPA2uiCatalog } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui/a2ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> catalog</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createMPA2uiCatalog</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;https://example.com/catalogs/orders/v1/catalog.json&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  components: [OrderTable]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The eighteen come along, and a name given twice is the later one — so passing an implementation called <code>Button</code> replaces the one here without rebuilding the list. Pass <code>MP_A2UI_EXTENDED_COMPONENTS</code> as well to start from all twenty-three. The individual implementations are exported too (<code>MPA2uiButton</code>, <code>MPA2uiText</code>, and so on), along with the five schemas (<code>DataTableApi</code>, <code>BarChartApi</code>, …), for a catalog assembled by hand.</p><h2 id="where-this-stops" tabindex="-1">Where this stops <a class="header-anchor" href="#where-this-stops" aria-label="Permalink to &quot;Where this stops&quot;">​</a></h2><p>Twenty-three components of a hundred and thirty. The command palette, the calendar, the tour, the animations and the rest are not reachable from a payload, because an agent can only ask for what a catalog names — and a component becomes nameable by being given a schema an agent can fill in from JSON alone, which is a decision per component rather than a switch.</p><p>The protocol is also young: v0.9.1 is current, v1.0 is a candidate, and the SDK is at 0.11. This subpath targets v0.9 and will follow the breaking changes the protocol makes; the components it draws with will not move underneath it.</p>`,50)])])}const g=e(n,[["render",o]]);export{k as __pageData,g as default};
