import{_ as h,C as a,o as r,c as l,j as e,a as d,E as s,a4 as i}from"./chunks/framework.CKd2nDfM.js";const f=JSON.parse('{"title":"MPScrollZone","description":"A strip of anything, laid out in one direction and scrolled in it. Cards, chips, avatars or thumbnails run across the box or down it, in as many lines as you…","frontmatter":{"title":"MPScrollZone","order":23},"headers":[],"relativePath":"components/layout/scroll-zone.md","filePath":"en/components/layout/scroll-zone.md","lastUpdated":1788505412000}'),p={name:"components/layout/scroll-zone.md"};function c(k,t,g,u,w,m){const n=a("Demo"),o=a("PropsTable");return r(),l("div",null,[t[0]||(t[0]=e("h1",{id:"mpscrollzone",tabindex:"-1"},[d("MPScrollZone "),e("a",{class:"header-anchor",href:"#mpscrollzone","aria-label":'Permalink to "MPScrollZone"'},"​")],-1)),t[1]||(t[1]=e("p",{class:"mp-lede"},"A strip of anything, laid out in one direction and scrolled in it. Cards, chips, avatars or thumbnails run across the box or down it, in as many lines as you ask for, with a pair of buttons for the pointer that has neither a wheel nor a finger.",-1)),s(n,{src:"scroll-zone/hero",minHeight:420}),t[2]||(t[2]=i(`<div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MPChip, MPScrollZone } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;material-plus-ui&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPScrollZone</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> label</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Categories&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPChip</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;Espresso&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPChip</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPChip</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;Filter&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPChip</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPChip</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;Cold brew&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPChip</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPScrollZone</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span></code></pre></div><h2 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-label="Permalink to &quot;Props&quot;">​</a></h2>`,2)),s(o,{name:"MPScrollZone"}),t[3]||(t[3]=i('<h2 id="it-is-a-scroll-container-and-only-ever-that" tabindex="-1">It is a scroll container, and only ever that <a class="header-anchor" href="#it-is-a-scroll-container-and-only-ever-that" aria-label="Permalink to &quot;It is a scroll container, and only ever that&quot;">​</a></h2><p>Swiping on a phone, two-finger dragging on a trackpad, the arrow keys and the scrollbar are the browser&#39;s own, and none of them is intercepted. What is added on top is a pair of buttons for the pointer that cannot do any of it, a mouse drag for the strip that reads as something to pull rather than something to page, and — only where <code>wheel</code> asks for it — the wheel turned onto the axis the strip runs along.</p><p>Nothing is transformed. A translated track would have to argue for an exception to the rule the rest of the library keeps, and a scroll offset does not: it is also what makes the strip run the other way under RTL without being told, what keeps the scrollbar honest, and what lets the browser scroll a focused child into view on its own.</p><h2 id="lines-is-what-separates-it-from-mpcarousel" tabindex="-1"><code>lines</code> is what separates it from <code>MPCarousel</code> <a class="header-anchor" href="#lines-is-what-separates-it-from-mpcarousel" aria-label="Permalink to &quot;`lines` is what separates it from `MPCarousel`&quot;">​</a></h2><p>A carousel is one thing at a time and knows which one. It has a <code>value</code>, an index, arrows that mean <em>the slide before this one</em>, and a row of marks saying where the reader is.</p><p>A scroll zone has none of those, because it is a shelf that happens to be longer than the room it is in. There is no current item to report — and two rows of thumbnails is a shape a carousel cannot hold at all:</p><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MPScrollZone</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> lines</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gap</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}&gt;</span></span></code></pre></div><p>That is a grid rather than a flex row, and deliberately: a wrapping row wraps at the box&#39;s edge, and what is wanted here is a fixed number of rows and as many columns as it takes.</p><h2 id="where-the-buttons-sit-decides-what-happens-at-an-end" tabindex="-1">Where the buttons sit decides what happens at an end <a class="header-anchor" href="#where-the-buttons-sit-decides-what-happens-at-an-end" aria-label="Permalink to &quot;Where the buttons sit decides what happens at an end&quot;">​</a></h2><p><code>inline</code> puts them beside the strip, in the layout, so the scroller stops where the button starts and an item is cut off at the button&#39;s edge rather than sliding under it. <code>overlay</code> puts them over the ends, which keeps every pixel of the box for content and lets an item pass under a button — what a shelf of pictures wants, where the thing under the button is a picture that carries on.</p><p>The lane an inline button sits in is held open even while that button has nowhere to go. A lane that came and went would resize the strip under the pointer each time it reached an end, and an emptied lane is not a lighter row — it is the same row reading as stray padding at the edge every reader meets first.</p><p>So <code>buttons=&quot;auto&quot;</code> does two different things, and the difference is what the absence would cost:</p><table tabindex="0"><thead><tr><th>Placement</th><th>Nothing overflows</th><th>At an end</th></tr></thead><tbody><tr><td><code>inline</code></td><td>No buttons</td><td>Drawn, <code>disabled</code></td></tr><tr><td><code>overlay</code></td><td>No buttons</td><td>Removed</td></tr></tbody></table><p><code>buttons=&quot;always&quot;</code> draws both from the first paint, <code>disabled</code> rather than gone at an end. That is what a strip whose content arrives from a fetch wants, so the buttons are not appearing under the pointer half a second in.</p><h2 id="the-wheel-is-the-page-s-until-you-ask-for-it" tabindex="-1">The wheel is the page&#39;s until you ask for it <a class="header-anchor" href="#the-wheel-is-the-page-s-until-you-ask-for-it" aria-label="Permalink to &quot;The wheel is the page&#39;s until you ask for it&quot;">​</a></h2><p>A wheel taken from the page is the page&#39;s: a reader who meant to scroll past the shelf is held by it instead, which is the most disliked thing a horizontal strip can do. So <code>wheel</code> is off by default.</p><p>Turned on, what it takes it gives back at the ends — a strip with nothing left ahead of it is something to scroll past rather than something to be caught in. A trackpad swiping sideways is left alone, because that already scrolls the strip and answering it here would move twice as far as it was asked to. A vertical zone ignores the prop entirely; there the wheel already points the way the strip runs.</p><h2 id="the-strip-is-a-tab-stop-with-a-name" tabindex="-1">The strip is a tab stop with a name <a class="header-anchor" href="#the-strip-is-a-tab-stop-with-a-name" aria-label="Permalink to &quot;The strip is a tab stop with a name&quot;">​</a></h2><p>It has to be one, or a reader with no pointer cannot move it at all. And a tab stop that announces nothing is worse than one that says only what kind of thing it is, so it carries <code>role=&quot;group&quot;</code> and a name either way.</p><p><code>label</code> is what is <em>in</em> the strip — &quot;Categories&quot;, &quot;Recent files&quot;. Without it the fallback says what it is, in whatever language <code>locale</code> names.</p><p>Everything the buttons can do, the keyboard can do too. In <code>hold</code> mode that includes the hold itself: Enter or Space held on the button travels for as long as it is down, which is the half a pointer-only implementation forgets.</p><h2 id="dragging-waits-for-the-pointer-to-mean-it" tabindex="-1">Dragging waits for the pointer to mean it <a class="header-anchor" href="#dragging-waits-for-the-pointer-to-mean-it" aria-label="Permalink to &quot;Dragging waits for the pointer to mean it&quot;">​</a></h2><p>Nothing is taken at the press — not the pointer capture, not the document&#39;s selection. A press on this strip is far more often a click on a card inside it, so the capture waits until the pointer has moved past a few pixels, which is what keeps a plain click able to select text and focus what it landed on.</p><p>Once it has moved, the click that <code>pointerup</code> is followed by is swallowed on the way down. Without that, letting go of a drag opens whatever card the strip happened to stop under.</p><p>Touch is left alone. A finger already scrolls, and the browser&#39;s own scrolling has momentum, rubber-banding and a scrollbar that no handler reproduces.</p>',25))])}const y=h(p,[["render",c]]);export{f as __pageData,y as default};
